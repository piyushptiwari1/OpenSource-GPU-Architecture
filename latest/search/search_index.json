{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"OpenGPU","text":"<p>A minimal, fully open-source 16-bit SIMT GPU implemented in SystemVerilog, with a complete CI toolchain, a C++ reference simulator, an executable ISA spec, and a DiffTest harness.</p> <p> </p>"},{"location":"#why","title":"Why","text":"<p>Most GPU architectures are proprietary. The few open implementations (Miaow, VeriGPU) target full feature-parity, which makes them hard to study. OpenGPU is the opposite end of the spectrum: small enough to read end-to-end, disciplined enough to ship.</p>"},{"location":"#what-you-get","title":"What you get","text":"Artefact Where Status 16-bit SIMT RTL <code>src/</code> \u2713 Canonical ISA YAML <code>docs/isa/instructions.yaml</code> \u2713 Auto-generated assembler grammar + C++ decode table <code>tools/codegen/</code> \u2713 C++ reference simulator (<code>opengpu-refsim</code>) <code>sim/cppref/</code> \u2713 DiffTest harness (RTL \u2194 refsim, JSONL retire stream) <code>tools/difftest/</code> \u2713 Pinned EDA toolchain image <code>ghcr.io/piyushptiwari1/opengpu-toolchain</code> \u2713 Cocotb module + e2e tests <code>test/</code> \u2713"},{"location":"#where-to-start","title":"Where to start","text":"<ul> <li>New here? Read the Architecture overview.</li> <li>Want the instruction set? See the ISA reference   (auto-rendered from YAML).</li> <li>Hacking on RTL? Start with the Build &amp; test workflow   and the cocotb overview.</li> <li>Tracing a kernel run? The matadd walkthrough   steps through retired instructions one at a time.</li> </ul>"},{"location":"#project-layout","title":"Project layout","text":"<pre><code>.\n\u251c\u2500\u2500 src/                    SystemVerilog RTL\n\u251c\u2500\u2500 test/                   Cocotb test benches (per module + e2e kernels)\n\u251c\u2500\u2500 sim/cppref/             C++ reference simulator (CMake + Catch2)\n\u251c\u2500\u2500 tools/codegen/          ISA YAML \u2192 C++ table + asm grammar + this site\n\u251c\u2500\u2500 tools/difftest/         JSONL stream comparator (RTL \u2194 refsim)\n\u251c\u2500\u2500 containers/toolchain/   Pinned OSS HDL/EDA image\n\u251c\u2500\u2500 docs/                   This site (mkdocs-material)\n\u2514\u2500\u2500 .github/workflows/      Lint, Sim, Refsim, Docs, Build-toolchain\n</code></pre>"},{"location":"#license","title":"License","text":"<p>MIT \u2014 see <code>LICENSE</code>.</p>"},{"location":"changelog/","title":"Changelog","text":"<p>The authoritative changelog is <code>CHANGES.md</code> in the repo root, which maps every merged upstream PR and resolved issue to the file changes that integrated it.</p> <p>This page summarises milestones at a higher level.</p>"},{"location":"changelog/#unreleased","title":"Unreleased","text":"<ul> <li>Step 5 \u2014 Documentation site (mkdocs-material) live on GitHub Pages,   with auto-rendered ISA reference and <code>mkdocstrings</code> API docs for the   Python tooling.</li> <li>Step 4 \u2014 Canonical ISA YAML, C++ reference simulator (<code>opengpu-refsim</code>),   DiffTest harness comparing RTL \u2194 refsim retire streams.</li> <li>Step 3 \u2014 Simulation CI rebuilt around the pinned toolchain image; 8/8   module tests green on every push.</li> <li>Step 2 \u2014 Reproducible toolchain Docker image   (<code>ghcr.io/piyushptiwari1/opengpu-toolchain</code>) and matching dev container.</li> <li>Step 1 \u2014 Repository governance, ruff + mypy + Verible + pyslang lint   gates, CODEOWNERS, contribution docs.</li> </ul>"},{"location":"cocotb-overview/","title":"cocotb in 10 minutes: where it sits in the simulation stack","text":"<p>This guide is for readers who already write C/C++ but are new to Verilog/SystemVerilog and to cocotb.</p> <p>It only tries to do three things:</p> <ol> <li>Explain where cocotb sits in the overall simulation flow.</li> <li>Explain which layer the RTL, the simulator and the Python tests each live in.</li> <li>Walk through one concrete <code>test_matadd</code> run end-to-end against this    repository.</li> </ol>"},{"location":"cocotb-overview/#1-one-line-intuition","title":"1. One-line intuition","text":"<p>You can roughly model the system like this:</p> <ul> <li>RTL / Verilog / SystemVerilog: the \"hardware program\" being tested.</li> <li>Simulator: the engine that executes that hardware description.</li> <li>cocotb: the bridge that lets Python talk to the simulator.</li> <li>Python test code: behaves like a testbench -- drives the DUT, waits for   clock edges, checks results.</li> </ul> <p>So cocotb is not the RTL itself, and not the simulator itself. It is the Python testing framework that sits between the two.</p>"},{"location":"cocotb-overview/#2-layered-view-who-is-on-top-of-whom","title":"2. Layered view: who is on top of whom","text":"<pre><code>flowchart TB\n    py[Python Test Code&lt;br/&gt;test/test_matadd.py&lt;br/&gt;test/helpers/*.py]\n    cocotb[cocotb Runtime&lt;br/&gt;Coroutine Scheduler&lt;br/&gt;Trigger API]\n    sim[Simulator Bridge&lt;br/&gt;VPI / VHPI / FLI]\n    iverilog[Icarus Runtime&lt;br/&gt;iverilog + vvp]\n    rtl[RTL Design&lt;br/&gt;src/*.sv&lt;br/&gt;Top module: gpu]\n    model[Simulated Signals and State&lt;br/&gt;clk done registers buses memories]\n\n    py --&gt; cocotb\n    cocotb --&gt; sim\n    sim --&gt; iverilog\n    iverilog --&gt; rtl\n    rtl --&gt; model\n    model --&gt; sim</code></pre> <p>Reading top-down:</p> <ol> <li>The top layer is your Python test, e.g. <code>test_matadd.py</code>.</li> <li>It calls cocotb APIs such as <code>RisingEdge</code>, <code>ReadOnly</code>, <code>@cocotb.test()</code>.</li> <li>cocotb talks to the simulator through a simulator interface (VPI etc.).</li> <li>The simulator executes the RTL underneath.</li> <li>The RTL produces signal values, register state, and bus values inside the    simulator.</li> </ol> <p>Python is therefore not \"executing Verilog directly.\" It observes and drives the DUT inside the simulator through cocotb.</p>"},{"location":"cocotb-overview/#3-end-to-end-flow-of-a-cocotb-test","title":"3. End-to-end flow of a cocotb test","text":"<pre><code>flowchart LR\n    a[SystemVerilog RTL&lt;br/&gt;src/*.sv] --&gt; b[sv2v&lt;br/&gt;convert to Verilog]\n    b --&gt; c[iverilog&lt;br/&gt;compile and elaborate]\n    c --&gt; d[vvp&lt;br/&gt;start simulation runtime]\n    d --&gt; e[cocotb loads Python test]\n    e --&gt; f[test_matadd starts]\n    f --&gt; g[drive reset and clock]\n    g --&gt; h[load program and data memory]\n    h --&gt; i[assert start and wait cycles]\n    i --&gt; j[read dut signals and logs]\n    j --&gt; k[assert final memory results]</code></pre> <p>A useful analogy:</p> <ul> <li><code>sv2v + iverilog + vvp</code> is responsible for bringing the hardware world to   life.</li> <li>cocotb is responsible for plugging the Python test into that world.</li> <li>Python code is responsible for stimulating inputs, observing outputs and   asserting expectations.</li> </ul>"},{"location":"cocotb-overview/#4-which-file-in-this-repo-lives-in-which-layer","title":"4. Which file in this repo lives in which layer?","text":""},{"location":"cocotb-overview/#hardware-layer","title":"Hardware layer","text":"<p>These files describe the design under test -- the GPU itself:</p> <ul> <li><code>src/gpu.sv</code></li> <li><code>src/core.sv</code></li> <li><code>src/decoder.sv</code></li> <li><code>src/alu.sv</code></li> <li>the rest of <code>src/*.sv</code></li> </ul> <p>Think of them as \"hardware source code.\"</p>"},{"location":"cocotb-overview/#test-layer","title":"Test layer","text":"<p>These files belong to the cocotb / Python test layer:</p> <ul> <li><code>test/test_matadd.py</code></li> <li><code>test/test_matmul.py</code></li> <li><code>test/helpers/setup.py</code></li> <li><code>test/helpers/memory.py</code></li> <li><code>test/helpers/logger.py</code></li> <li><code>test/helpers/format.py</code></li> </ul> <p>They do not implement any GPU hardware; they play the role of testbench.</p>"},{"location":"cocotb-overview/#build-simulation-layer","title":"Build / simulation layer","text":"<p>These pieces compile the design and run it:</p> <ul> <li><code>Makefile</code></li> <li><code>sv2v</code></li> <li><code>iverilog</code></li> <li><code>vvp</code></li> </ul> <p>This layer is closer to a build system + runtime environment.</p>"},{"location":"cocotb-overview/#5-what-exactly-is-dut","title":"5. What exactly is <code>dut</code>?","text":"<p>In the test entry point you will see:</p> <pre><code>@cocotb.test()\nasync def test_matadd(dut):\n</code></pre> <p><code>dut</code> here is:</p> <ul> <li>not a Python object you constructed yourself,</li> <li>not a plain data structure,</li> <li>it is a DUT handle that cocotb passed in.</li> </ul> <p><code>DUT</code> is short for Device Under Test.</p> <p>You can think of <code>dut</code> as:</p> <p>A Python-side \"handle\" that points to the top-level Verilog instance.</p> <p>That is why code like the following works:</p> <pre><code>await RisingEdge(dut.clk)\nwhile dut.done.value != 1:\n    ...\n</code></pre> <p>It means:</p> <ul> <li><code>dut.clk</code>: access the DUT's clock signal.</li> <li><code>dut.done</code>: access the DUT's done signal.</li> <li><code>dut.&lt;name&gt;</code>: access any port or hierarchical object named <code>&lt;name&gt;</code> on the DUT.</li> </ul> <p>If you reach for a C++ analogy, it behaves like \"a top-level object handle exposed by the simulator.\"</p>"},{"location":"cocotb-overview/#6-why-does-the-python-code-constantly-convert-between-strings-and-ints","title":"6. Why does the Python code constantly convert between strings and ints?","text":"<p>This is the part that confuses software-background newcomers the most.</p> <p>The short answer:</p> <p>Verilog signals are bit vectors, not strings. But in cocotb tests we often turn a signal value into a string temporarily, because slicing, printing and inspecting are easier that way.</p> <p>For example, <code>test/helpers/memory.py</code> contains code like:</p> <pre><code>mem_read_address_bits = str(self.mem_read_address.value)\naddress_slice = mem_read_address_bits[i : i + self.addr_bits]\nmem_read_address.append(int(address_slice, 2))\n</code></pre> <p>What those three steps do:</p> <ol> <li>Convert the bus value into a string, e.g. <code>\"0000010100000011\"</code>.</li> <li>Slice out one lane's worth of address bits.</li> <li>Convert that slice back to an integer so it can index a Python list.</li> </ol> <p>So the picture is:</p> <ul> <li>Hardware layer: it is a sequence of bits.</li> <li>Python observation layer: it is rendered as a string for slicing.</li> <li>Python compute layer: it is parsed back into an int for indexing /   arithmetic.</li> </ul> <p>This is a very common testbench idiom.</p>"},{"location":"cocotb-overview/#7-division-of-labour-during-one-test_matadd-run","title":"7. Division of labour during one <code>test_matadd</code> run","text":""},{"location":"cocotb-overview/#what-the-rtl-does","title":"What the RTL does","text":"<p>The RTL implements the actual hardware behaviour:</p> <ul> <li>instruction fetch</li> <li>decode</li> <li>register read/write</li> <li>memory accesses</li> <li>ALU operations</li> <li>finally driving <code>done</code> high</li> </ul> <p>All of this happens inside <code>src/*.sv</code>.</p>"},{"location":"cocotb-overview/#what-cocotb-python-does","title":"What cocotb / Python does","text":"<p>The Python test is responsible for:</p> <ul> <li>bringing up the clock and applying reset</li> <li>pre-loading program memory</li> <li>pre-loading data memory</li> <li>calling <code>data_memory.run()</code> to service memory handshakes</li> <li>waiting for <code>dut.done</code></li> <li>dumping logs</li> <li>asserting that the final memory contents are correct</li> </ul> <p>In other words, Python is not \"implementing the GPU algorithm.\" It is driving and validating the GPU.</p>"},{"location":"cocotb-overview/#8-where-does-testhelpersmemorypy-fit-in","title":"8. Where does <code>test/helpers/memory.py</code> fit in?","text":"<p>This file is important because it is not a real RAM in the RTL -- it is a Python-side memory model built into the testbench.</p> <p>The data flow looks like:</p> <pre><code>RTL issues read / write requests\n    -&gt; Memory.run() in cocotb consumes those requests\n    -&gt; the Python list `self.memory` plays the role of \"software RAM\"\n    -&gt; Memory.run() drives read_data / ready back onto DUT signals\n</code></pre> <p>So:</p> <ul> <li>The hardware core lives inside the RTL.</li> <li>A non-trivial part of the program/data memory in the testbench actually   lives on the Python side.</li> </ul> <p>This often surprises engineers from a software background. You might assume \"everything is in Verilog,\" but in a cocotb environment peripheral models, memory models and driver logic are frequently written in Python.</p>"},{"location":"cocotb-overview/#9-a-practical-mental-model","title":"9. A practical mental model","text":"<p>If you have written C++ unit tests before, this mapping helps:</p> <ul> <li>RTL is like the \"library under test.\"</li> <li>Simulator is like the \"runtime that runs that library.\"</li> <li>cocotb is like the \"adapter that plugs Python tests into that runtime.\"</li> <li>Python test is like \"test code + mocks + assertions.\"</li> </ul> <p>But add a few hardware-specific twists:</p> <ul> <li>It is not function-call sequencing; it is clock-driven concurrent logic.</li> <li>The \"objects\" are not regular variables; they are signals and bit vectors.</li> <li>A bit is not always <code>0</code> or <code>1</code>; you may also see <code>X</code> and <code>Z</code>.</li> </ul>"},{"location":"cocotb-overview/#10-the-full-real-path-through-this-repo","title":"10. The full real path through this repo","text":"<p>Putting all the real names together:</p> <ol> <li><code>make test_matadd</code></li> <li><code>sv2v</code> converts <code>src/*.sv</code> into plain Verilog.</li> <li><code>iverilog</code> compiles the Verilog into a simulation image.</li> <li><code>vvp</code> starts the simulation.</li> <li>cocotb loads <code>test/test_matadd.py</code>.</li> <li>cocotb passes the elaborated top-level instance into <code>test_matadd(dut)</code>.</li> <li><code>setup()</code> raises the clock, applies reset and pre-loads memories.</li> <li>Each cycle, <code>Memory.run()</code> services program and data memory requests.</li> <li>The Python test waits on <code>RisingEdge(dut.clk)</code> until <code>dut.done == 1</code>.</li> <li>The test then inspects the log and the final memory to decide pass/fail.</li> </ol>"},{"location":"cocotb-overview/#11-three-questions-to-ask-while-reading-cocotb-code","title":"11. Three questions to ask while reading cocotb code","text":"<p>Whenever you read a piece of cocotb code, ask yourself:</p> <ol> <li>Is this code driving the DUT, or observing it?</li> <li>Is this operating on a Python variable, or on a DUT signal?</li> <li>Is this happening before, during or after a particular clock edge?</li> </ol> <p>Once those three are clear, cocotb code becomes much easier to follow.</p>"},{"location":"cocotb-overview/#12-the-takeaways-worth-remembering-right-now","title":"12. The takeaways worth remembering right now","text":"<ul> <li>cocotb is a Python testing framework, not RTL and not a simulator.</li> <li>The RTL lives in <code>src/*.sv</code> and is responsible for hardware behaviour.</li> <li>Python lives in <code>test/*.py</code> and is responsible for driving, modelling,   observing and asserting.</li> <li><code>dut</code> is the \"design-under-test handle\" cocotb injects into your test.</li> <li>Signals are fundamentally bit vectors; the strings and ints you see are   just convenient representations the Python side uses for processing.</li> </ul>"},{"location":"setup-verilog-simulation/","title":"tiny-gpu Setup Guide for a Systems Software Engineer","text":"<p>This guide is written for someone who is comfortable with Linux, shells, compilers, and build systems, but is new to Verilog/SystemVerilog simulation.</p> <p>It covers:</p> <ol> <li>what this repository is doing at a high level</li> <li>what tools are required</li> <li>how to install them</li> <li>the exact compatibility issues I hit on this machine</li> <li>how to compile and run the simulations successfully</li> </ol>"},{"location":"setup-verilog-simulation/#1-mental-model-what-you-are-actually-running","title":"1. Mental model: what you are actually running","text":"<p>If you come from systems software, the easiest analogy is this:</p> <ul> <li>SystemVerilog RTL (<code>src/*.sv</code>) is the hardware design source code</li> <li><code>sv2v</code> is a source-to-source translator that converts SystemVerilog into plain Verilog</li> <li><code>iverilog</code> is the compiler/elaborator for the generated Verilog</li> <li><code>vvp</code> is the simulation runtime that executes the compiled design</li> <li><code>cocotb</code> is the Python test harness that plays the role of a software testbench</li> </ul> <p>So the flow is roughly:</p> <pre><code>SystemVerilog source\n  -&gt; sv2v\nVerilog output\n  -&gt; iverilog\ncompiled simulation image (.vvp)\n  -&gt; vvp + cocotb Python test\nsimulated execution + assertions + logs\n</code></pre> <p>In this repo, cocotb acts like the \u201chost system\u201d for the GPU. It:</p> <ul> <li>starts the clock</li> <li>resets the DUT</li> <li>loads program memory and data memory</li> <li>writes the thread count into the device control register</li> <li>asserts <code>start</code></li> <li>waits until <code>done</code></li> <li>checks the output values</li> </ul>"},{"location":"setup-verilog-simulation/#2-what-this-repository-needs","title":"2. What this repository needs","text":"<p>From the repo's <code>README.md</code> and <code>Makefile</code>, the practical requirements are:</p> <ul> <li>Python 3</li> <li><code>pip</code></li> <li><code>make</code></li> <li><code>sv2v</code></li> <li><code>iverilog</code></li> <li><code>vvp</code> (comes with Icarus Verilog)</li> <li><code>cocotb</code></li> <li>a <code>build/</code> directory</li> </ul> <p>Optional:</p> <ul> <li><code>gtkwave</code> for waveform viewing</li> </ul>"},{"location":"setup-verilog-simulation/#3-important-compatibility-note-cocotb-version-matters-here","title":"3. Important compatibility note: cocotb version matters here","text":"<p>This repository's <code>Makefile</code> uses:</p> <pre><code>cocotb-config --prefix\n</code></pre> <p>That works with cocotb 1.9.x, but it does not work with cocotb 2.0.x, where <code>--prefix</code> was removed from <code>cocotb-config</code>.</p>"},{"location":"setup-verilog-simulation/#what-happened-on-this-machine","title":"What happened on this machine","text":"<p>I first installed <code>cocotb 2.0.1</code>, and the repo became incompatible with the existing <code>Makefile</code>.</p> <p>The working fix was to pin cocotb to:</p> <pre><code>cocotb==1.9.2\n</code></pre> <p>If you keep the current <code>Makefile</code> unchanged, use cocotb 1.9.2.</p>"},{"location":"setup-verilog-simulation/#4-recommended-install-paths","title":"4. Recommended install paths","text":"<p>There are two reasonable setup modes.</p>"},{"location":"setup-verilog-simulation/#option-a-easiest-if-you-have-sudo","title":"Option A \u2014 easiest if you have sudo","text":"<p>Install system packages via apt, and install cocotb with pip:</p> <pre><code>sudo apt-get update\nsudo apt-get install -y iverilog gtkwave unzip curl\npython3 -m pip install --user 'cocotb==1.9.2'\n</code></pre> <p>Then install <code>sv2v</code> from the official release:</p> <pre><code>mkdir -p \"$HOME/.local/opt/downloads\"\ncurl -L https://github.com/zachjs/sv2v/releases/download/v0.0.13/sv2v-Linux.zip \\\n  -o \"$HOME/.local/opt/downloads/sv2v-Linux.zip\"\nunzip -o \"$HOME/.local/opt/downloads/sv2v-Linux.zip\" \\\n  -d \"$HOME/.local/opt/downloads/sv2v-linux\"\nln -sfn \"$HOME/.local/opt/downloads/sv2v-linux/sv2v-Linux/sv2v\" \"$HOME/.local/bin/sv2v\"\n</code></pre> <p>This is the simplest route if you control the machine.</p>"},{"location":"setup-verilog-simulation/#option-b-no-sudo-user-local-install-only","title":"Option B \u2014 no sudo / user-local install only","text":"<p>This is what I used successfully on this machine.</p>"},{"location":"setup-verilog-simulation/#5-no-sudo-setup-that-actually-worked-here","title":"5. No-sudo setup that actually worked here","text":""},{"location":"setup-verilog-simulation/#51-install-cocotb-in-your-user-site-packages","title":"5.1 Install cocotb in your user site-packages","text":"<pre><code>python3 -m pip install --user --force-reinstall 'cocotb==1.9.2'\n</code></pre> <p>Verify:</p> <pre><code>cocotb-config --version\ncocotb-config --help | head\n</code></pre> <p>Expected: version <code>1.9.2</code>, and help output should include <code>--prefix</code>.</p>"},{"location":"setup-verilog-simulation/#52-download-and-unpack-icarus-verilog-locally","title":"5.2 Download and unpack Icarus Verilog locally","text":"<p>Create directories:</p> <pre><code>mkdir -p \"$HOME/.local/opt/iverilog\"\nmkdir -p \"$HOME/.local/opt/downloads\"\n</code></pre> <p>Download the Ubuntu package without installing it system-wide:</p> <pre><code>cd /path/to/tiny-gpu\napt-get download iverilog\n</code></pre> <p>That produces a file like:</p> <pre><code>iverilog_11.0-1.1_amd64.deb\n</code></pre> <p>Extract it into your home directory:</p> <pre><code>dpkg-deb -x ./iverilog_11.0-1.1_amd64.deb \"$HOME/.local/opt/iverilog\"\n</code></pre>"},{"location":"setup-verilog-simulation/#53-fix-the-internal-helper-path-for-the-extracted-icarus-package","title":"5.3 Fix the internal helper path for the extracted Icarus package","text":"<p>This matters.</p> <p>When you install <code>iverilog</code> normally with apt, the package layout and hard-coded helper paths line up automatically. But when you simply extract the <code>.deb</code> under your home directory, the main <code>iverilog</code> binary still expects helper programs under a slightly different prefix.</p> <p>Without this fix, <code>iverilog</code> fails like this:</p> <pre><code>ivlpp: not found\nivl: not found\n</code></pre> <p>Create a compatibility symlink:</p> <pre><code>mkdir -p \"$HOME/.local/opt/iverilog/usr/x86_64-linux-gnu\"\nln -sfn ../lib/x86_64-linux-gnu/ivl \"$HOME/.local/opt/iverilog/usr/x86_64-linux-gnu/ivl\"\n</code></pre>"},{"location":"setup-verilog-simulation/#54-download-the-official-sv2v-linux-release","title":"5.4 Download the official sv2v Linux release","text":"<pre><code>curl -L https://github.com/zachjs/sv2v/releases/download/v0.0.13/sv2v-Linux.zip \\\n  -o \"$HOME/.local/opt/downloads/sv2v-Linux.zip\"\n\nunzip -o \"$HOME/.local/opt/downloads/sv2v-Linux.zip\" \\\n  -d \"$HOME/.local/opt/downloads/sv2v-linux\"\n</code></pre> <p>The binary ends up here:</p> <pre><code>$HOME/.local/opt/downloads/sv2v-linux/sv2v-Linux/sv2v\n</code></pre>"},{"location":"setup-verilog-simulation/#55-export-path-for-this-repo-session","title":"5.5 Export PATH for this repo session","text":"<pre><code>export PATH=\"$HOME/.local/opt/iverilog/usr/bin:$HOME/.local/opt/downloads/sv2v-linux/sv2v-Linux:$HOME/.local/bin:$PATH\"\n</code></pre> <p>Verify the full toolchain:</p> <pre><code>iverilog -V\nvvp -V\nsv2v --version\ncocotb-config --version\n</code></pre> <p>The working versions on this machine were:</p> <ul> <li><code>iverilog</code> 11.0</li> <li><code>vvp</code> 11.0</li> <li><code>sv2v</code> v0.0.13</li> <li><code>cocotb</code> 1.9.2</li> </ul>"},{"location":"setup-verilog-simulation/#6-running-the-repo-for-the-first-time","title":"6. Running the repo for the first time","text":"<p>From the repo root:</p> <pre><code>mkdir -p build\nmake test_matadd\nmake test_matmul\n</code></pre>"},{"location":"setup-verilog-simulation/#what-these-targets-do","title":"What these targets do","text":"<p><code>make test_matadd</code> and <code>make test_matmul</code> both do the following:</p> <ol> <li><code>make compile</code></li> <li>run <code>sv2v</code></li> <li>compile <code>build/gpu.v</code> with <code>iverilog</code></li> <li>run the compiled simulation with <code>vvp</code></li> <li>load the relevant cocotb Python test module</li> </ol>"},{"location":"setup-verilog-simulation/#expected-successful-output","title":"Expected successful output","text":"<p>You should see cocotb output like:</p> <pre><code>Running on Icarus Verilog version 11.0 (stable)\nRunning tests with cocotb v1.9.2\n...\nPASS\n</code></pre> <p>On this machine, both of these passed successfully:</p> <ul> <li><code>make test_matadd</code></li> <li><code>make test_matmul</code></li> </ul> <p>One small repo oddity: <code>test/test_matmul.py</code> exports a test function named <code>test_matadd</code>, so the second run still reports <code>running test_matadd</code>. That is a naming mismatch in the repo, not an environment issue.</p>"},{"location":"setup-verilog-simulation/#7-files-and-outputs-you-should-expect","title":"7. Files and outputs you should expect","text":""},{"location":"setup-verilog-simulation/#build-outputs","title":"Build outputs","text":"<p>The build places generated artifacts in:</p> <pre><code>build/\n</code></pre> <p>Important files include:</p> <ul> <li><code>build/alu.v</code></li> <li><code>build/gpu.v</code></li> <li><code>build/sim.vvp</code></li> </ul>"},{"location":"setup-verilog-simulation/#log-outputs","title":"Log outputs","text":"<p>The Python logger writes execution logs under:</p> <pre><code>test/logs/\n</code></pre> <p>These logs are useful when you want to inspect:</p> <ul> <li>the initial memory state</li> <li>the instruction-by-instruction trace</li> <li>final memory contents</li> </ul>"},{"location":"setup-verilog-simulation/#8-what-each-tool-is-doing-in-plain-software-engineering-terms","title":"8. What each tool is doing, in plain software-engineering terms","text":""},{"location":"setup-verilog-simulation/#sv2v","title":"<code>sv2v</code>","text":"<p>Think of this as a compatibility transpiler.</p> <p>The source code is written in SystemVerilog, but the simulation compiler used here (<code>iverilog</code>) is happiest with plain Verilog in this workflow. So <code>sv2v</code> translates the source before compilation.</p>"},{"location":"setup-verilog-simulation/#iverilog","title":"<code>iverilog</code>","text":"<p>Think of this as the compile + elaboration step.</p> <p>It takes the generated Verilog and produces a simulation image (<code>.vvp</code>) containing the hardware design.</p>"},{"location":"setup-verilog-simulation/#vvp","title":"<code>vvp</code>","text":"<p>Think of this as the runtime loader / executor for the compiled simulation image.</p>"},{"location":"setup-verilog-simulation/#cocotb","title":"<code>cocotb</code>","text":"<p>Think of this as a Python-based integration test harness for hardware.</p> <p>Instead of writing HDL testbench code, you write Python coroutines that:</p> <ul> <li>drive signals</li> <li>wait on edges or time</li> <li>observe outputs</li> <li>assert expected behavior</li> </ul>"},{"location":"setup-verilog-simulation/#9-known-rough-edges-in-this-repository","title":"9. Known rough edges in this repository","text":"<p>These are worth knowing up front so you do not waste time blaming yourself:</p> <ol> <li><code>build/</code> is not created automatically</li> <li> <p>you must run <code>mkdir -p build</code></p> </li> <li> <p>The current Makefile assumes cocotb 1.x behavior</p> </li> <li>specifically <code>cocotb-config --prefix</code></li> <li> <p>cocotb 2.0 breaks this assumption</p> </li> <li> <p>The <code>sv2v</code> invocation is unusual</p> </li> <li>the Makefile uses:      <pre><code>sv2v -I src/* -w build/gpu.v\n</code></pre></li> <li> <p>this is not the clearest standard <code>sv2v</code> usage pattern, but it worked in this repo during validation</p> </li> <li> <p><code>test_matmul.py</code> has a naming mismatch</p> </li> <li>the module is correct</li> <li>the exported test function name is confusing</li> </ol>"},{"location":"setup-verilog-simulation/#10-recommended-shell-snippet-for-future-sessions","title":"10. Recommended shell snippet for future sessions","text":"<p>If you plan to work on this repo repeatedly without sudo, add something like this to your shell rc file:</p> <pre><code>export PATH=\"$HOME/.local/opt/iverilog/usr/bin:$HOME/.local/opt/downloads/sv2v-linux/sv2v-Linux:$HOME/.local/bin:$PATH\"\n</code></pre> <p>Then open a new shell and verify:</p> <pre><code>iverilog -V\nsv2v --version\ncocotb-config --version\n</code></pre>"},{"location":"setup-verilog-simulation/#11-fast-sanity-check-checklist","title":"11. Fast sanity-check checklist","text":"<p>If you just want the minimum sequence to confirm the environment works:</p> <pre><code>export PATH=\"$HOME/.local/opt/iverilog/usr/bin:$HOME/.local/opt/downloads/sv2v-linux/sv2v-Linux:$HOME/.local/bin:$PATH\"\nmkdir -p build\ncocotb-config --version\niverilog -V\nsv2v --version\nmake test_matadd\nmake test_matmul\n</code></pre> <p>If both tests pass, your local simulation environment is usable.</p>"},{"location":"setup-verilog-simulation/#12-where-to-look-next-if-you-are-new-to-rtl","title":"12. Where to look next if you are new to RTL","text":"<p>If you want to understand the code after setup, read in this order:</p> <ol> <li><code>README.md</code> \u2014 project overview and architecture explanation</li> <li><code>src/gpu.sv</code> \u2014 top-level module</li> <li><code>src/core.sv</code> \u2014 per-core composition</li> <li><code>src/scheduler.sv</code> \u2014 control flow and execution stages</li> <li><code>test/helpers/setup.py</code> \u2014 how software launches the design</li> <li><code>test/test_matadd.py</code> \u2014 the simplest end-to-end example</li> </ol> <p>That order maps well to a systems engineer\u2019s instincts: start with the top-level architecture, then look at orchestration, then execution, then the test harness.</p>"},{"location":"isa/reference/","title":"ISA reference","text":"<p>Generated</p> <p>This page is rendered from <code>docs/isa/instructions.yaml</code> by <code>tools/codegen/render_isa_md.py</code>. Edit the YAML, not this file.</p> <p><code>SHA-256(instructions.yaml)</code> = <code>9e27eb5918cf8cd82ece0fb7d318a65c184af7acd7b5fcecf4acfe71b9c33c8f</code></p>"},{"location":"isa/reference/#overview","title":"Overview","text":"<p>OpenGPU (<code>opengpu</code>, ISA version 1) is a 16-bit instruction-width SIMT machine with an 8-bit register / data path and an 8-bit unified address space. It defines 12 instructions across 4 encoding classes (B, I, R, Z).</p>"},{"location":"isa/reference/#register-file","title":"Register file","text":"<p>16 general-purpose 8-bit registers per thread, plus a 3-bit NZP flag register written by <code>CMP</code> and consumed by <code>BRnzp</code>.</p> ID Name Alias Read-only <code>0</code> <code>R0</code> \u2014 <code>1</code> <code>R1</code> \u2014 <code>2</code> <code>R2</code> \u2014 <code>3</code> <code>R3</code> \u2014 <code>4</code> <code>R4</code> \u2014 <code>5</code> <code>R5</code> \u2014 <code>6</code> <code>R6</code> \u2014 <code>7</code> <code>R7</code> \u2014 <code>8</code> <code>R8</code> \u2014 <code>9</code> <code>R9</code> \u2014 <code>10</code> <code>R10</code> \u2014 <code>11</code> <code>R11</code> \u2014 <code>12</code> <code>R12</code> \u2014 <code>13</code> <code>R13</code> <code>%blockIdx</code> \u2713 <code>14</code> <code>R14</code> <code>%blockDim</code> \u2713 <code>15</code> <code>R15</code> <code>%threadIdx</code> \u2713"},{"location":"isa/reference/#encoding-classes","title":"Encoding classes","text":"Type Layout (<code>instr[15:0]</code>) <code>B</code> <code>[15:12]=opcode | [11:9]=nzp | [8:0]=target</code> <code>I</code> <code>[15:12]=opcode | [11:8]=Rd | [7:0]=imm8</code> <code>R</code> <code>[15:12]=opcode | [11:8]=Rd | [7:4]=Rs | [3:0]=Rt</code> <code>Z</code> <code>[15:12]=opcode | [11:0]=0  (NOP / RET)</code>"},{"location":"isa/reference/#control-signals","title":"Control signals","text":"<p>Every instruction emits a fixed bundle of control signals (mirrors <code>src/decoder.sv</code> outputs). Bits not listed default to zero. The C++ reference simulator decodes the same bundle from the auto-generated <code>sim/cppref/include/opengpu/isa_table.hpp</code>.</p> Signal Width <code>reg_write_enable</code> 1 <code>mem_read_enable</code> 1 <code>mem_write_enable</code> 1 <code>nzp_write_enable</code> 1 <code>reg_input_mux</code> 2 <code>alu_arithmetic_mux</code> 2 <code>alu_output_mux</code> 1 <code>pc_mux</code> 1 <code>ret</code> 1"},{"location":"isa/reference/#instructions","title":"Instructions","text":""},{"location":"isa/reference/#nop-opcode-0x0-z-type","title":"<code>NOP</code> \u2014 opcode <code>0x0</code> (Z-type)","text":"<ul> <li>Syntax: <code>NOP</code></li> <li>Semantics: no-op</li> <li>Control: <code>alu_arithmetic_mux=0</code>, <code>alu_output_mux=0</code>, <code>mem_read_enable=0</code>, <code>mem_write_enable=0</code>, <code>nzp_write_enable=0</code>, <code>pc_mux=0</code>, <code>reg_input_mux=0</code>, <code>reg_write_enable=0</code>, <code>ret=0</code></li> </ul>"},{"location":"isa/reference/#brnzp-opcode-0x1-b-type","title":"<code>BRnzp</code> \u2014 opcode <code>0x1</code> (B-type)","text":"<ul> <li>Syntax: <code>BRnzp &lt;nzp3&gt;, &lt;imm9&gt;</code></li> <li>Semantics: if (NZP &amp; nzp_mask) PC &lt;- imm9</li> <li>Control: <code>alu_arithmetic_mux=0</code>, <code>alu_output_mux=0</code>, <code>mem_read_enable=0</code>, <code>mem_write_enable=0</code>, <code>nzp_write_enable=0</code>, <code>pc_mux=1</code>, <code>reg_input_mux=0</code>, <code>reg_write_enable=0</code>, <code>ret=0</code></li> </ul>"},{"location":"isa/reference/#cmp-opcode-0x2-r-type","title":"<code>CMP</code> \u2014 opcode <code>0x2</code> (R-type)","text":"<ul> <li>Syntax: <code>CMP Rs, Rt</code></li> <li>Semantics: NZP &lt;- {RsRt} <li>Control: <code>alu_arithmetic_mux=0</code>, <code>alu_output_mux=1</code>, <code>mem_read_enable=0</code>, <code>mem_write_enable=0</code>, <code>nzp_write_enable=1</code>, <code>pc_mux=0</code>, <code>reg_input_mux=0</code>, <code>reg_write_enable=0</code>, <code>ret=0</code></li>"},{"location":"isa/reference/#add-opcode-0x3-r-type","title":"<code>ADD</code> \u2014 opcode <code>0x3</code> (R-type)","text":"<ul> <li>Syntax: <code>ADD Rd, Rs, Rt</code></li> <li>Semantics: Rd &lt;- Rs + Rt   (mod 2^8)</li> <li>Control: <code>alu_arithmetic_mux=0</code>, <code>alu_output_mux=0</code>, <code>mem_read_enable=0</code>, <code>mem_write_enable=0</code>, <code>nzp_write_enable=0</code>, <code>pc_mux=0</code>, <code>reg_input_mux=0</code>, <code>reg_write_enable=1</code>, <code>ret=0</code></li> </ul>"},{"location":"isa/reference/#sub-opcode-0x4-r-type","title":"<code>SUB</code> \u2014 opcode <code>0x4</code> (R-type)","text":"<ul> <li>Syntax: <code>SUB Rd, Rs, Rt</code></li> <li>Semantics: Rd &lt;- Rs - Rt   (mod 2^8)</li> <li>Control: <code>alu_arithmetic_mux=1</code>, <code>alu_output_mux=0</code>, <code>mem_read_enable=0</code>, <code>mem_write_enable=0</code>, <code>nzp_write_enable=0</code>, <code>pc_mux=0</code>, <code>reg_input_mux=0</code>, <code>reg_write_enable=1</code>, <code>ret=0</code></li> </ul>"},{"location":"isa/reference/#mul-opcode-0x5-r-type","title":"<code>MUL</code> \u2014 opcode <code>0x5</code> (R-type)","text":"<ul> <li>Syntax: <code>MUL Rd, Rs, Rt</code></li> <li>Semantics: Rd &lt;- (Rs * Rt) &amp; 0xFF</li> <li>Control: <code>alu_arithmetic_mux=2</code>, <code>alu_output_mux=0</code>, <code>mem_read_enable=0</code>, <code>mem_write_enable=0</code>, <code>nzp_write_enable=0</code>, <code>pc_mux=0</code>, <code>reg_input_mux=0</code>, <code>reg_write_enable=1</code>, <code>ret=0</code></li> </ul>"},{"location":"isa/reference/#div-opcode-0x6-r-type","title":"<code>DIV</code> \u2014 opcode <code>0x6</code> (R-type)","text":"<ul> <li>Syntax: <code>DIV Rd, Rs, Rt</code></li> <li>Semantics: Rd &lt;- (Rt == 0) ? 0 : (Rs / Rt)</li> <li>Control: <code>alu_arithmetic_mux=3</code>, <code>alu_output_mux=0</code>, <code>mem_read_enable=0</code>, <code>mem_write_enable=0</code>, <code>nzp_write_enable=0</code>, <code>pc_mux=0</code>, <code>reg_input_mux=0</code>, <code>reg_write_enable=1</code>, <code>ret=0</code></li> </ul>"},{"location":"isa/reference/#ldr-opcode-0x7-r-type","title":"<code>LDR</code> \u2014 opcode <code>0x7</code> (R-type)","text":"<ul> <li>Syntax: <code>LDR Rd, Rs</code></li> <li>Semantics: Rd &lt;- mem[Rs]</li> <li>Control: <code>alu_arithmetic_mux=0</code>, <code>alu_output_mux=0</code>, <code>mem_read_enable=1</code>, <code>mem_write_enable=0</code>, <code>nzp_write_enable=0</code>, <code>pc_mux=0</code>, <code>reg_input_mux=1</code>, <code>reg_write_enable=1</code>, <code>ret=0</code></li> </ul>"},{"location":"isa/reference/#str-opcode-0x8-r-type","title":"<code>STR</code> \u2014 opcode <code>0x8</code> (R-type)","text":"<ul> <li>Syntax: <code>STR Rs, Rt</code></li> <li>Semantics: mem[Rs] &lt;- Rt</li> <li>Control: <code>alu_arithmetic_mux=0</code>, <code>alu_output_mux=0</code>, <code>mem_read_enable=0</code>, <code>mem_write_enable=1</code>, <code>nzp_write_enable=0</code>, <code>pc_mux=0</code>, <code>reg_input_mux=0</code>, <code>reg_write_enable=0</code>, <code>ret=0</code></li> </ul>"},{"location":"isa/reference/#const-opcode-0x9-i-type","title":"<code>CONST</code> \u2014 opcode <code>0x9</code> (I-type)","text":"<ul> <li>Syntax: <code>CONST Rd, #imm8</code></li> <li>Semantics: Rd &lt;- imm8</li> <li>Control: <code>alu_arithmetic_mux=0</code>, <code>alu_output_mux=0</code>, <code>mem_read_enable=0</code>, <code>mem_write_enable=0</code>, <code>nzp_write_enable=0</code>, <code>pc_mux=0</code>, <code>reg_input_mux=2</code>, <code>reg_write_enable=1</code>, <code>ret=0</code></li> </ul>"},{"location":"isa/reference/#atomicadd-opcode-0xa-r-type","title":"<code>ATOMICADD</code> \u2014 opcode <code>0xA</code> (R-type)","text":"<ul> <li>Syntax: <code>ATOMICADD Rd, Rs, Rt</code></li> <li>Semantics: old = mem[Rs]; mem[Rs] &lt;- (old + Rt) &amp; 0xFF; Rd &lt;- old</li> <li>Control: <code>alu_arithmetic_mux=0</code>, <code>alu_output_mux=0</code>, <code>mem_read_enable=1</code>, <code>mem_write_enable=1</code>, <code>nzp_write_enable=0</code>, <code>pc_mux=0</code>, <code>reg_input_mux=1</code>, <code>reg_write_enable=1</code>, <code>ret=0</code></li> </ul>"},{"location":"isa/reference/#ret-opcode-0xf-z-type","title":"<code>RET</code> \u2014 opcode <code>0xF</code> (Z-type)","text":"<ul> <li>Syntax: <code>RET</code></li> <li>Semantics: thread halts (decoded_ret asserted)</li> <li>Control: <code>alu_arithmetic_mux=0</code>, <code>alu_output_mux=0</code>, <code>mem_read_enable=0</code>, <code>mem_write_enable=0</code>, <code>nzp_write_enable=0</code>, <code>pc_mux=0</code>, <code>reg_input_mux=0</code>, <code>reg_write_enable=0</code>, <code>ret=1</code></li> </ul>"},{"location":"modules/","title":"tiny-gpu Independent Module Guide","text":"<p>These notes explain both:</p> <ul> <li>the small, independent building-block modules in <code>src/</code></li> <li>the two integration layers that assemble them: <code>core.sv</code> and <code>gpu.sv</code></li> </ul> <p>They are meant to be read side by side with the SystemVerilog source. The focus is not just \"what this line does,\" but what role the module plays in the whole GPU and how to mentally trace it over time.</p> <p>This set is grounded in:</p> <ul> <li>the actual RTL in <code>src/*.sv</code></li> <li>the repo's DeepWiki architecture pages (<code>Overview</code>, <code>Architecture Overview</code>, <code>Execution Model</code>, <code>Hardware Modules</code>, <code>Memory System</code>)</li> </ul>"},{"location":"modules/#recommended-reading-order","title":"Recommended reading order","text":"<ol> <li><code>scheduler.md</code> \u2014 the global per-instruction rhythm of a core</li> <li><code>decoder.md</code> \u2014 how instruction bits become control signals</li> <li><code>fetcher.md</code> \u2014 instruction fetch handshake</li> <li><code>registers.md</code> \u2014 operand reads and writeback timing</li> <li><code>alu.md</code> \u2014 arithmetic and compare execution</li> <li><code>pc.md</code> \u2014 branch and NZP behavior</li> <li><code>lsu.md</code> \u2014 per-thread data-memory access</li> <li><code>controller.md</code> \u2014 how many requesters share few memory channels</li> <li><code>dispatch.md</code> \u2014 how thread_count becomes blocks on cores</li> <li><code>dcr.md</code> \u2014 where launch metadata comes from</li> <li><code>core.md</code> \u2014 how one core combines shared control with replicated thread lanes</li> <li><code>gpu.md</code> \u2014 how the whole chip is wired together</li> </ol>"},{"location":"modules/#module-list","title":"Module list","text":"<ul> <li><code>alu.md</code></li> <li><code>controller.md</code></li> <li><code>dcr.md</code></li> <li><code>decoder.md</code></li> <li><code>dispatch.md</code></li> <li><code>fetcher.md</code></li> <li><code>core.md</code></li> <li><code>gpu.md</code></li> <li><code>lsu.md</code></li> <li><code>pc.md</code></li> <li><code>registers.md</code></li> <li><code>scheduler.md</code></li> </ul>"},{"location":"modules/#what-is-intentionally-not-covered-here","title":"What is intentionally not covered here","text":"<ul> <li>advanced GPU topics like warp scheduling, coalescing, or branch divergence handling</li> </ul> <p>Those are better understood after the smaller modules feel natural.</p>"},{"location":"modules/alu/","title":"ALU Module","text":"<p>Source: <code>src/alu.sv</code></p>"},{"location":"modules/alu/#what-this-module-is","title":"What this module is","text":"<p><code>alu.sv</code> is the per-thread arithmetic unit. Each active thread lane inside a core gets its own ALU instance, so all threads can execute the same arithmetic instruction in parallel on different register values.</p> <p>In DeepWiki terms, this is one of the thread execution units inside the core's replicated datapath.</p>"},{"location":"modules/alu/#where-it-sits-in-tiny-gpu","title":"Where it sits in tiny-gpu","text":"<ul> <li>Upstream: <code>registers.sv</code> provides <code>rs</code> and <code>rt</code>; <code>decoder.sv</code> provides ALU control bits; <code>scheduler.sv</code> provides the shared <code>core_state</code></li> <li>Downstream: <code>registers.sv</code> may write <code>alu_out</code> back into <code>rd</code>; <code>pc.sv</code> uses the low 3 bits during <code>CMP</code></li> </ul>"},{"location":"modules/alu/#clockreset-and-when-work-happens","title":"Clock/reset and when work happens","text":"<ul> <li>Synchronous module: work happens on <code>posedge clk</code></li> <li>Reset clears the stored output register</li> <li>Useful work only happens when:</li> <li><code>enable == 1</code></li> <li><code>core_state == EXECUTE (3'b101)</code></li> </ul>"},{"location":"modules/alu/#interface-cheat-sheet","title":"Interface cheat sheet","text":"Port group Meaning <code>clk</code>, <code>reset</code> standard sequential timing <code>enable</code> disables unused thread lanes in a partially full block <code>core_state</code> stage gating from the scheduler <code>decoded_alu_arithmetic_mux</code> selects ADD / SUB / MUL / DIV <code>decoded_alu_output_mux</code> selects arithmetic path vs compare path <code>rs</code>, <code>rt</code> source operands from the register file <code>alu_out</code> final registered ALU result"},{"location":"modules/alu/#diagram","title":"Diagram","text":"<pre><code>flowchart TD\n    A[\"Clock edge arrives\"] --&gt; B{\"reset?\"}\n    B -- yes --&gt; C[\"Clear stored ALU output register\"]\n    B -- no --&gt; D{\"enable == 1 and core_state == EXECUTE?\"}\n    D -- no --&gt; E[\"Hold previous alu_out_reg value\"]\n    D -- yes --&gt; F{\"decoded_alu_output_mux\"}\n\n    F -- compare path --&gt; G[\"Compare rs vs rt&lt;br/&gt;compute gt, eq, lt bits\"]\n    G --&gt; H[\"Pack compare flags into low result bits\"]\n    H --&gt; M[\"Write result into alu_out_reg\"]\n\n    F -- arithmetic path --&gt; I{\"decoded_alu_arithmetic_mux\"}\n    I -- ADD --&gt; J[\"Compute rs + rt\"]\n    I -- SUB --&gt; K[\"Compute rs - rt\"]\n    I -- MUL --&gt; L[\"Compute rs * rt\"]\n    I -- DIV --&gt; N[\"Compute rs / rt\"]\n    J --&gt; M\n    K --&gt; M\n    L --&gt; M\n    N --&gt; M\n\n    M --&gt; O[\"Expose registered output as alu_out\"]\n    C --&gt; O\n    E --&gt; O</code></pre>"},{"location":"modules/alu/#behavior-walkthrough","title":"Behavior walkthrough","text":"<ol> <li>The scheduler moves the whole core into <code>EXECUTE</code>.</li> <li>The decoder has already chosen which ALU behavior is needed.</li> <li>If the instruction is normal arithmetic, the ALU uses the arithmetic mux.</li> <li>If the instruction is <code>CMP</code>, the ALU does not return a normal arithmetic result. Instead, it packs comparison flags into the low 3 bits.</li> <li>The result is written into <code>alu_out_reg</code>, so the output is registered, not purely combinational.</li> </ol>"},{"location":"modules/alu/#decision-logic-to-focus-on","title":"Decision logic to focus on","text":"<ul> <li>First gate: only act in <code>EXECUTE</code></li> <li>Second gate: arithmetic result vs compare result</li> <li>Third gate: which arithmetic sub-operation to apply</li> </ul> <p>The important beginner insight is that the ALU is not choosing instructions directly. It only reacts to the control signals already produced by the decoder.</p>"},{"location":"modules/alu/#timing-notes","title":"Timing notes","text":"<ul> <li><code>rs</code> and <code>rt</code> are prepared earlier by <code>registers.sv</code></li> <li><code>alu_out</code> becomes meaningful after the <code>EXECUTE</code> edge</li> <li>For <code>CMP</code>, <code>pc.sv</code> later samples <code>alu_out[2:0]</code> during <code>UPDATE</code> to refresh the NZP register</li> </ul>"},{"location":"modules/alu/#common-pitfalls","title":"Common pitfalls","text":"<ul> <li>Thinking <code>CMP</code> writes a normal number into a general register. It does not.</li> <li>Thinking the ALU always runs every cycle. It is stage-gated.</li> <li>Missing that <code>alu_out</code> is backed by <code>alu_out_reg</code>, so old values persist until overwritten.</li> </ul>"},{"location":"modules/alu/#trace-it-yourself","title":"Trace-it-yourself","text":"<p>Try the instruction <code>ADD R6, R4, R5</code>:</p> <ol> <li>In <code>REQUEST</code>, the register file snapshots <code>R4</code> and <code>R5</code> into <code>rs</code> and <code>rt</code></li> <li>In <code>EXECUTE</code>, the ALU sees arithmetic mode + ADD sub-op</li> <li>On the clock edge, it stores <code>rs + rt</code> into <code>alu_out_reg</code></li> <li>In <code>UPDATE</code>, the register file writes that value into <code>R6</code></li> </ol>"},{"location":"modules/alu/#read-next","title":"Read next","text":"<ul> <li><code>registers.md</code></li> <li><code>pc.md</code></li> <li><code>decoder.md</code></li> </ul>"},{"location":"modules/controller/","title":"Controller Module","text":"<p>Source: <code>src/controller.sv</code></p>"},{"location":"modules/controller/#what-this-module-is","title":"What this module is","text":"<p><code>controller.sv</code> is the memory traffic manager. It sits between many internal requesters and a smaller number of external memory channels.</p> <p>The same module is reused for both:</p> <ul> <li>program memory requests from fetchers</li> <li>data memory requests from LSUs</li> </ul> <p>DeepWiki's memory-system description matches this role exactly: the controller is the bandwidth gatekeeper and response relay.</p>"},{"location":"modules/controller/#where-it-sits-in-tiny-gpu","title":"Where it sits in tiny-gpu","text":"<ul> <li>Upstream: fetchers or LSUs behave like \"consumers\"</li> <li>Downstream: external program memory or data memory</li> <li>Sibling concept: one controller instance can have several independent channels, and each channel has its own internal state</li> </ul>"},{"location":"modules/controller/#clockreset-and-when-work-happens","title":"Clock/reset and when work happens","text":"<ul> <li>Entirely synchronous on <code>posedge clk</code></li> <li>Reset clears all valid/ready outputs and returns every channel to <code>IDLE</code></li> <li>After reset, each channel repeatedly:</li> <li>looks for a request</li> <li>forwards it to memory</li> <li>waits for memory</li> <li>relays completion back to the right consumer</li> </ul>"},{"location":"modules/controller/#interface-cheat-sheet","title":"Interface cheat sheet","text":"Port group Meaning <code>consumer_read_*</code> incoming read requests from fetchers/LSUs <code>consumer_write_*</code> incoming write requests from LSUs <code>consumer_*_ready</code> completion signal back to the original requester <code>mem_read_*</code> outgoing read request to external memory <code>mem_write_*</code> outgoing write request to external memory <code>current_consumer[]</code> which requester each channel is currently serving <code>channel_serving_consumer</code> prevents two channels from claiming the same requester"},{"location":"modules/controller/#diagram","title":"Diagram","text":"<pre><code>stateDiagram-v2\n    [*] --&gt; IDLE\n    IDLE --&gt; IDLE: scan consumers&lt;br/&gt;skip already-claimed requests\n    IDLE --&gt; READ_WAITING: claim one read requester&lt;br/&gt;record current_consumer&lt;br/&gt;raise mem_read_valid\n    IDLE --&gt; WRITE_WAITING: claim one write requester&lt;br/&gt;record current_consumer&lt;br/&gt;raise mem_write_valid\n\n    READ_WAITING --&gt; READ_WAITING: keep request active&lt;br/&gt;until mem_read_ready\n    READ_WAITING --&gt; READ_RELAYING: memory returns read data&lt;br/&gt;drop mem_read_valid&lt;br/&gt;raise consumer_read_ready\n\n    WRITE_WAITING --&gt; WRITE_WAITING: keep request active&lt;br/&gt;until mem_write_ready\n    WRITE_WAITING --&gt; WRITE_RELAYING: memory accepts write&lt;br/&gt;drop mem_write_valid&lt;br/&gt;raise consumer_write_ready\n\n    READ_RELAYING --&gt; READ_RELAYING: keep consumer_read_ready high&lt;br/&gt;until consumer drops read_valid\n    READ_RELAYING --&gt; IDLE: release claimed consumer\n\n    WRITE_RELAYING --&gt; WRITE_RELAYING: keep consumer_write_ready high&lt;br/&gt;until consumer drops write_valid\n    WRITE_RELAYING --&gt; IDLE: release claimed consumer</code></pre>"},{"location":"modules/controller/#behavior-walkthrough","title":"Behavior walkthrough","text":"<ol> <li>Each memory channel behaves like a tiny worker.</li> <li>While idle, a channel scans consumers looking for a request not already claimed by another channel.</li> <li>Once found, the channel records <code>current_consumer[i]</code> and raises the corresponding memory-side valid signal.</li> <li>It then waits for external memory to acknowledge/return data.</li> <li>After memory responds, the controller raises the corresponding consumer-side <code>ready</code> signal.</li> <li>It does not immediately forget the request. It waits for the consumer to drop its <code>valid</code>, which serves as the acknowledgement.</li> <li>Only then does the channel return to <code>IDLE</code> and become free again.</li> </ol>"},{"location":"modules/controller/#state-machine-idea","title":"State machine idea","text":"<ul> <li><code>IDLE</code>: channel is free and looking for work</li> <li><code>READ_WAITING</code>: a read request has been forwarded to memory</li> <li><code>WRITE_WAITING</code>: a write request has been forwarded to memory</li> <li><code>READ_RELAYING</code>: read data is ready and being presented back to the consumer</li> <li><code>WRITE_RELAYING</code>: write completion is being presented back to the consumer</li> </ul>"},{"location":"modules/controller/#timing-notes","title":"Timing notes","text":"<ul> <li><code>valid</code> and <code>ready</code> are not one-cycle pulses by accident here; they are part of a handshake lifecycle</li> <li><code>channel_serving_consumer</code> is important because multiple channels are updated in the same clocked block</li> <li>The relay states intentionally hold <code>consumer_*_ready</code> high until the requester lowers <code>valid</code></li> </ul>"},{"location":"modules/controller/#common-pitfalls","title":"Common pitfalls","text":"<ul> <li>Thinking <code>ready</code> means \"the channel is idle.\" Here it often means \"your request has completed.\"</li> <li>Missing that there are two sides of handshake: consumer side and memory side.</li> <li>Forgetting this module can represent either program-memory traffic or data-memory traffic depending on parameters.</li> </ul>"},{"location":"modules/controller/#trace-it-yourself","title":"Trace-it-yourself","text":"<p>Imagine one LSU asserts <code>consumer_read_valid[3]</code>:</p> <ol> <li>A free channel in <code>IDLE</code> claims consumer 3</li> <li>It copies <code>consumer_read_address[3]</code> to <code>mem_read_address[i]</code></li> <li>It waits in <code>READ_WAITING</code></li> <li>When <code>mem_read_ready[i]</code> arrives, it copies <code>mem_read_data[i]</code> back into <code>consumer_read_data[3]</code></li> <li>It raises <code>consumer_read_ready[3]</code></li> <li>When the LSU drops <code>consumer_read_valid[3]</code>, the channel goes back to <code>IDLE</code></li> </ol>"},{"location":"modules/controller/#read-next","title":"Read next","text":"<ul> <li><code>fetcher.md</code></li> <li><code>lsu.md</code></li> <li><code>scheduler.md</code></li> </ul>"},{"location":"modules/core/","title":"Core Module","text":"<p>Source: <code>src/core.sv</code></p>"},{"location":"modules/core/#what-this-module-is","title":"What this module is","text":"<p><code>core.sv</code> is the main compute engine for one block of threads. If the smaller module docs explain the parts, this file explains how those parts are assembled into one working SIMD-style core.</p> <p>The key beginner mental model is this:</p> <ul> <li>one shared control path per core</li> <li>many replicated thread lanes per core</li> </ul> <p>So the core behaves like one instruction stream controlling several per-thread datapaths in parallel.</p> <p>This matches the repo's DeepWiki architecture model: the core contains a fetcher, decoder, scheduler, and per-thread execution units.</p>"},{"location":"modules/core/#where-it-sits-in-tiny-gpu","title":"Where it sits in tiny-gpu","text":"<ul> <li>Upstream: <code>dispatch.sv</code> starts the core on a specific block and tells it <code>block_id</code> and <code>thread_count</code></li> <li>Inside the core:</li> <li>shared modules: <code>fetcher</code>, <code>decoder</code>, <code>scheduler</code></li> <li>per-thread modules: <code>registers</code>, <code>alu</code>, <code>lsu</code>, <code>pc</code></li> <li>Downstream:</li> <li>program-memory controller sees fetch traffic</li> <li>data-memory controller sees LSU traffic</li> <li>dispatcher sees <code>done</code></li> </ul>"},{"location":"modules/core/#clockreset-and-when-work-happens","title":"Clock/reset and when work happens","text":"<ul> <li>Entire module is synchronous through its submodules</li> <li><code>reset</code> resets the whole core and all its internal submodules</li> <li><code>start</code> tells the scheduler to begin executing the assigned block</li> <li>The scheduler drives the per-instruction rhythm using <code>core_state</code></li> </ul>"},{"location":"modules/core/#interface-cheat-sheet","title":"Interface cheat sheet","text":"Group Meaning <code>start</code>, <code>done</code> per-block launch and completion handshake <code>block_id</code>, <code>thread_count</code> metadata for the currently assigned block <code>program_mem_*</code> one shared instruction fetch interface for the whole core <code>data_mem_read_*</code>, <code>data_mem_write_*</code> per-thread data-memory interfaces for LSU traffic <code>core_state</code>, <code>instruction</code>, decoded signals shared control-path signals inside the core <code>rs/rt</code>, <code>alu_out</code>, <code>lsu_out</code>, <code>next_pc</code> arrays per-thread lane datapath signals"},{"location":"modules/core/#diagram","title":"Diagram","text":"<pre><code>flowchart TD\n    subgraph SharedControl[\"Shared per-core control path\"]\n        S[\"scheduler\"] --&gt; F[\"fetcher\"]\n        F --&gt; D[\"decoder\"]\n        D --&gt; CTRL[\"decoded control bundle\"]\n        S --&gt; STAGE[\"core_state\"]\n        S --&gt; CPC[\"current_pc\"]\n    end\n\n    subgraph ThreadLanes[\"Replicated per-thread lane i\"]\n        R0[\"registers lane i\"] --&gt; A0[\"alu lane i\"]\n        R0 --&gt; L0[lsu lane i]\n        A0 --&gt; R0\n        L0 --&gt; R0\n        A0 --&gt; P0[\"pc lane i\"]\n        P0 --&gt; NPC[\"next_pc for lane i\"]\n    end\n\n    CTRL --&gt; R0\n    CTRL --&gt; A0\n    CTRL --&gt; L0\n    CTRL --&gt; P0\n    STAGE --&gt; R0\n    STAGE --&gt; A0\n    STAGE --&gt; L0\n    STAGE --&gt; P0\n    CPC --&gt; F\n    CPC --&gt; P0\n    F --&gt; PMEM[\"program memory interface\"]\n    L0 --&gt; DMEM[\"data memory interface for this lane\"]\n    NPC --&gt; MERGE[\"scheduler assumes active lanes converge\"]\n    MERGE --&gt; S</code></pre>"},{"location":"modules/core/#how-to-read-this-file","title":"How to read this file","text":"<p>This file is mostly an integration file. It does not invent a lot of new behavior. Instead, it answers these questions:</p> <ol> <li>Which modules are shared per core?</li> <li>Which modules are duplicated per thread lane?</li> <li>How do the shared control signals fan out to all lanes?</li> <li>How do the per-lane outputs feed back into shared control?</li> </ol> <p>That is why <code>core.sv</code> has many wires/regs and many module instantiations, but relatively little algorithmic logic of its own.</p>"},{"location":"modules/core/#behavior-walkthrough","title":"Behavior walkthrough","text":"<ol> <li>The dispatcher gives this core a <code>block_id</code> and <code>thread_count</code>.</li> <li>The scheduler starts in charge of the block's instruction lifecycle.</li> <li>The fetcher retrieves one instruction from program memory using the shared <code>current_pc</code>.</li> <li>The decoder turns that instruction into shared control signals.</li> <li>Those shared control signals are broadcast to all active thread lanes.</li> <li>Inside each lane:</li> <li><code>registers</code> provides operands</li> <li><code>alu</code> computes arithmetic or compare results</li> <li><code>lsu</code> performs memory access if needed</li> <li><code>pc</code> computes that lane's <code>next_pc</code></li> <li>The scheduler later decides whether the block is done or moves to the next instruction.</li> </ol>"},{"location":"modules/core/#shared-path-vs-replicated-path","title":"Shared path vs replicated path","text":"<p>This is the most important structural idea in the file.</p>"},{"location":"modules/core/#shared-per-core-pieces","title":"Shared per-core pieces","text":"<ul> <li><code>fetcher</code></li> <li><code>decoder</code></li> <li><code>scheduler</code></li> <li>one shared <code>instruction</code></li> <li>one shared <code>current_pc</code></li> <li>one shared bundle of decoded control signals</li> </ul> <p>These exist only once per core because the tiny-gpu executes one instruction stream per block.</p>"},{"location":"modules/core/#replicated-per-thread-pieces","title":"Replicated per-thread pieces","text":"<p>Inside the <code>generate</code> loop, every thread lane gets its own:</p> <ul> <li><code>alu</code></li> <li><code>lsu</code></li> <li><code>registers</code></li> <li><code>pc</code></li> </ul> <p>This is how the same instruction can operate on different thread-local data at the same time.</p>"},{"location":"modules/core/#the-generate-loop","title":"The <code>generate</code> loop","text":"<p>The most important code pattern in this file is:</p> <pre><code>for (i = 0; i &lt; THREADS_PER_BLOCK; i = i + 1) begin : threads\n    ...\nend\n</code></pre> <p>This does not mean \"loop at runtime\" the way a software <code>for</code> loop does.</p> <p>It means:</p> <ul> <li>build hardware lane 0</li> <li>build hardware lane 1</li> <li>build hardware lane 2</li> <li>...</li> </ul> <p>So if <code>THREADS_PER_BLOCK = 4</code>, the generated hardware really contains 4 ALUs, 4 LSUs, 4 register files, and 4 PCs.</p>"},{"location":"modules/core/#partial-block-handling","title":"Partial block handling","text":"<p>Not every block uses every physical lane.</p> <p>That is why each thread-local module gets:</p> <pre><code>.enable(i &lt; thread_count)\n</code></pre> <p>Meaning:</p> <ul> <li>if this is a full block, all lanes are enabled</li> <li>if this is the final partial block, only the first <code>thread_count</code> lanes are active</li> </ul> <p>This is how one physical core can still execute a short last block safely.</p>"},{"location":"modules/core/#the-key-simplification-converged-pc","title":"The key simplification: converged PC","text":"<p>Look closely at this structure:</p> <ul> <li>each lane computes <code>next_pc[i]</code></li> <li>but the scheduler later chooses one representative <code>next_pc</code></li> </ul> <p>This reflects one of the repo's biggest simplifications:</p> <p>all active threads in a block are assumed to converge back to the same PC</p> <p>Real GPUs must deal with branch divergence much more carefully.</p> <p>So <code>core.sv</code> is the place where the repo's simplified SIMD control model becomes most visible.</p>"},{"location":"modules/core/#timing-notes","title":"Timing notes","text":"<ul> <li>Source operand arrays <code>rs[i]</code>, <code>rt[i]</code> are filled by <code>registers.sv</code></li> <li><code>alu_out[i]</code>, <code>lsu_out[i]</code>, <code>next_pc[i]</code> are per-lane outputs consumed later in the instruction lifecycle</li> <li>Decoded signals are shared because the decoder runs once per core, not once per thread</li> </ul>"},{"location":"modules/core/#common-pitfalls","title":"Common pitfalls","text":"<ul> <li>Thinking <code>core.sv</code> contains the actual arithmetic/memory algorithms. Most of those live in submodules.</li> <li>Thinking the <code>generate</code> loop is software-style iteration. It is hardware replication.</li> <li>Forgetting that this core processes one block at a time, not one thread at a time.</li> <li>Missing the difference between:</li> <li>shared control state (<code>core_state</code>, <code>instruction</code>, <code>current_pc</code>)</li> <li>per-thread datapath state (<code>rs[i]</code>, <code>registers</code>, <code>lsu_state[i]</code>, <code>next_pc[i]</code>)</li> </ul>"},{"location":"modules/core/#trace-it-yourself","title":"Trace-it-yourself","text":"<p>Try tracing one <code>ADD</code> instruction for a block with 4 active threads:</p> <ol> <li>Scheduler moves to <code>FETCH</code></li> <li>Fetcher returns one instruction word</li> <li>Decoder produces one shared arithmetic-control bundle</li> <li>All 4 register files snapshot their own <code>rs</code> and <code>rt</code></li> <li>All 4 ALUs compute in parallel</li> <li>All 4 register files independently write back their own <code>alu_out</code></li> </ol> <p>Same instruction, different per-thread data.</p>"},{"location":"modules/core/#read-next","title":"Read next","text":"<ul> <li><code>gpu.md</code></li> <li><code>scheduler.md</code></li> <li><code>registers.md</code></li> </ul>"},{"location":"modules/dcr/","title":"DCR Module","text":"<p>Source: <code>src/dcr.sv</code></p>"},{"location":"modules/dcr/#what-this-module-is","title":"What this module is","text":"<p><code>dcr.sv</code> is the Device Control Register. In this tiny GPU, it is a very small configuration module whose main job is to remember the total <code>thread_count</code> for the next kernel launch.</p> <p>This is one of the simplest modules in the repo, but it is conceptually important because it shows how software/testbench configuration becomes hardware state.</p>"},{"location":"modules/dcr/#where-it-sits-in-tiny-gpu","title":"Where it sits in tiny-gpu","text":"<ul> <li>Upstream: host/testbench writes launch metadata through <code>device_control_write_enable</code> and <code>device_control_data</code></li> <li>Downstream: <code>dispatch.sv</code> reads <code>thread_count</code></li> </ul>"},{"location":"modules/dcr/#clockreset-and-when-work-happens","title":"Clock/reset and when work happens","text":"<ul> <li>Synchronous on <code>posedge clk</code></li> <li>Reset clears the internal register to zero</li> <li>If <code>device_control_write_enable</code> is high, the module latches the new control value</li> </ul>"},{"location":"modules/dcr/#interface-cheat-sheet","title":"Interface cheat sheet","text":"Port group Meaning <code>device_control_write_enable</code> host says \"store this launch setting now\" <code>device_control_data</code> 8-bit configuration payload <code>thread_count</code> current stored thread count for dispatch"},{"location":"modules/dcr/#diagram","title":"Diagram","text":"<pre><code>flowchart TD\n    A[posedge clk] --&gt; B{reset?}\n    B -- yes --&gt; C[device_control_register = 0]\n    B -- no --&gt; D{device_control_write_enable?}\n    D -- yes --&gt; E[latch device_control_data]\n    D -- no --&gt; F[hold previous value]\n    E --&gt; G[thread_count = stored register]\n    F --&gt; G\n    C --&gt; G</code></pre>"},{"location":"modules/dcr/#behavior-walkthrough","title":"Behavior walkthrough","text":"<ol> <li>The host/testbench chooses how many total threads the kernel should launch.</li> <li>It drives <code>device_control_data</code> and asserts <code>device_control_write_enable</code>.</li> <li><code>dcr.sv</code> stores that byte internally.</li> <li>The output <code>thread_count</code> continuously reflects the stored value.</li> <li>Later, the dispatcher uses it to calculate how many blocks to issue.</li> </ol>"},{"location":"modules/dcr/#decision-logic-to-focus-on","title":"Decision logic to focus on","text":"<p>There is almost no state machine here. The important idea is simply:</p> <ul> <li>reset clears launch metadata</li> <li>write-enable updates launch metadata</li> <li>otherwise the last configuration is preserved</li> </ul>"},{"location":"modules/dcr/#timing-notes","title":"Timing notes","text":"<ul> <li><code>thread_count</code> is not recomputed each time; it is just the stored register contents</li> <li>If software never writes the DCR after reset, the dispatcher sees zero threads</li> </ul>"},{"location":"modules/dcr/#common-pitfalls","title":"Common pitfalls","text":"<ul> <li>Overthinking it as a complex control block. It is really just a tiny configuration register.</li> <li>Forgetting that this register belongs to the GPU launch path, not to per-thread execution.</li> </ul>"},{"location":"modules/dcr/#trace-it-yourself","title":"Trace-it-yourself","text":"<p>If the testbench writes <code>8</code> into <code>device_control_data</code> with <code>device_control_write_enable = 1</code>, then after the clock edge <code>thread_count</code> becomes <code>8</code>. The dispatcher can then divide those 8 threads into blocks.</p>"},{"location":"modules/dcr/#read-next","title":"Read next","text":"<ul> <li><code>dispatch.md</code></li> <li><code>scheduler.md</code></li> </ul>"},{"location":"modules/decoder/","title":"Decoder Module","text":"<p>Source: <code>src/decoder.sv</code></p>"},{"location":"modules/decoder/#what-this-module-is","title":"What this module is","text":"<p><code>decoder.sv</code> converts the raw 16-bit instruction into fields and control signals. It is the module that answers the question: \"What should the rest of the core do for this instruction?\"</p> <p>DeepWiki describes the decoder as part of the core's shared control path. That is the right mental model: one decoder drives all active threads in a core at once.</p>"},{"location":"modules/decoder/#where-it-sits-in-tiny-gpu","title":"Where it sits in tiny-gpu","text":"<ul> <li>Upstream: <code>fetcher.sv</code> provides <code>instruction</code>; <code>scheduler.sv</code> provides <code>core_state</code></li> <li>Downstream: <code>registers.sv</code>, <code>alu.sv</code>, <code>lsu.sv</code>, <code>pc.sv</code>, and <code>scheduler.sv</code> all consume decoded signals</li> </ul>"},{"location":"modules/decoder/#clockreset-and-when-work-happens","title":"Clock/reset and when work happens","text":"<ul> <li>Synchronous on <code>posedge clk</code></li> <li>Reset clears all remembered decode outputs</li> <li>Real decoding only happens in <code>DECODE</code> stage: <code>core_state == 3'b010</code></li> </ul>"},{"location":"modules/decoder/#interface-cheat-sheet","title":"Interface cheat sheet","text":"Group Meaning <code>instruction</code> the raw 16-bit instruction word <code>decoded_rd/rs/rt_address</code> register fields extracted from instruction bits <code>decoded_nzp</code>, <code>decoded_immediate</code> branch and immediate fields <code>decoded_reg_write_enable</code> register file should write in UPDATE <code>decoded_mem_read_enable</code>, <code>decoded_mem_write_enable</code> LSU should perform LDR/STR path <code>decoded_alu_*</code> ALU should do arithmetic or compare path <code>decoded_pc_mux</code> PC should use branch logic <code>decoded_ret</code> scheduler should finish block execution"},{"location":"modules/decoder/#diagram","title":"Diagram","text":"<pre><code>flowchart TD\n    A[\"DECODE stage begins\"] --&gt; B[\"Slice instruction into opcode, rd, rs, rt, nzp, immediate\"]\n    B --&gt; C[\"Clear all decoded control signals to safe defaults\"]\n    C --&gt; D{\"opcode field\"}\n\n    D --&gt;|NOP| E[\"Leave all controls low\"]\n    D --&gt;|BRnzp| F[\"Tell PC logic to use branch decision path\"]\n    D --&gt;|CMP| G[\"Select ALU compare output&lt;br/&gt;enable NZP write path\"]\n    D --&gt;|ADD SUB MUL DIV| H[\"Enable register writeback&lt;br/&gt;select arithmetic ALU sub-operation\"]\n    D --&gt;|LDR| I[\"Enable memory read&lt;br/&gt;route returned value into register writeback\"]\n    D --&gt;|STR| J[\"Enable memory write only\"]\n    D --&gt;|CONST| K[\"Enable register writeback from immediate\"]\n    D --&gt;|RET| L[\"Raise decoded_ret for scheduler\"]\n\n    F --&gt; M[\"PC module\"]\n    G --&gt; N[\"ALU and PC modules\"]\n    H --&gt; O[\"ALU and register file\"]\n    I --&gt; P[\"LSU and register file\"]\n    J --&gt; Q[\"LSU\"]\n    K --&gt; R[\"Register file\"]\n    L --&gt; S[\"Scheduler\"]</code></pre>"},{"location":"modules/decoder/#behavior-walkthrough","title":"Behavior walkthrough","text":"<ol> <li>The fetcher has already captured the current instruction.</li> <li>In <code>DECODE</code>, the decoder slices reusable bit fields out of that instruction.</li> <li>It then resets all control outputs to zero.</li> <li>Based on the opcode, it asserts only the control bits needed by that instruction.</li> <li>Those control bits will influence other modules in later stages.</li> </ol>"},{"location":"modules/decoder/#decision-logic-to-focus-on","title":"Decision logic to focus on","text":"<p>The most important design pattern here is:</p> <ol> <li>extract fields</li> <li>clear all controls</li> <li>set only what the selected opcode needs</li> </ol> <p>That avoids stale control signals leaking from the previous instruction.</p>"},{"location":"modules/decoder/#timing-notes","title":"Timing notes","text":"<ul> <li>The decoder does not itself perform arithmetic, memory access, or branching</li> <li>It only prepares control information for later stages</li> <li>Several instruction formats reuse the same raw bit slices differently, which is normal in instruction-set design</li> </ul>"},{"location":"modules/decoder/#common-pitfalls","title":"Common pitfalls","text":"<ul> <li>Thinking \"decode\" and \"execute\" are the same moment. They are separate stages.</li> <li>Forgetting to notice the default-zero control pattern.</li> <li>Reading <code>decoded_immediate</code> as meaningful for every opcode. It is only used when relevant.</li> </ul>"},{"location":"modules/decoder/#trace-it-yourself","title":"Trace-it-yourself","text":"<p>Take <code>LDR R4, R4</code>:</p> <ol> <li>The decoder extracts <code>rd = R4</code>, <code>rs = R4</code></li> <li>It clears all controls</li> <li>It asserts:</li> <li><code>decoded_reg_write_enable = 1</code></li> <li><code>decoded_reg_input_mux = MEMORY</code></li> <li><code>decoded_mem_read_enable = 1</code></li> <li>Later the LSU performs the load, and the register file writes the loaded value into <code>R4</code></li> </ol>"},{"location":"modules/decoder/#read-next","title":"Read next","text":"<ul> <li><code>scheduler.md</code></li> <li><code>registers.md</code></li> <li><code>lsu.md</code></li> </ul>"},{"location":"modules/dispatch/","title":"Dispatch Module","text":"<p>Source: <code>src/dispatch.sv</code></p>"},{"location":"modules/dispatch/#what-this-module-is","title":"What this module is","text":"<p><code>dispatch.sv</code> is the kernel-level work distributor. It takes one total <code>thread_count</code>, turns that into blocks, and hands those blocks to available cores.</p> <p>DeepWiki's execution-model and hardware-module pages describe the dispatcher as the block manager between the host configuration and the cores. That is exactly the right way to read this file.</p>"},{"location":"modules/dispatch/#where-it-sits-in-tiny-gpu","title":"Where it sits in tiny-gpu","text":"<ul> <li>Upstream: <code>dcr.sv</code> provides <code>thread_count</code>; external launch logic provides <code>start</code></li> <li>Downstream: cores receive <code>core_start</code>, <code>core_reset</code>, <code>core_block_id</code>, <code>core_thread_count</code></li> <li>Feedback: cores return <code>core_done</code></li> </ul>"},{"location":"modules/dispatch/#clockreset-and-when-work-happens","title":"Clock/reset and when work happens","text":"<ul> <li>Synchronous on <code>posedge clk</code></li> <li>Reset clears completion counters and puts all cores into reset</li> <li>Once <code>start</code> is observed, dispatch begins managing block assignment until all blocks are done</li> </ul>"},{"location":"modules/dispatch/#interface-cheat-sheet","title":"Interface cheat sheet","text":"Group Meaning <code>start</code> launch this kernel <code>thread_count</code> total threads requested for the kernel <code>core_done[]</code> each core reports when its current block is complete <code>core_start[]</code> tells a core to begin its assigned block <code>core_reset[]</code> resets a core between blocks <code>core_block_id[]</code> which block the core is executing <code>core_thread_count[]</code> how many threads are active in that block <code>done</code> whole kernel is finished"},{"location":"modules/dispatch/#diagram","title":"Diagram","text":"<pre><code>flowchart TD\n    A[\"Kernel start observed\"] --&gt; B[\"Compute total_blocks from thread_count and THREADS_PER_BLOCK\"]\n    B --&gt; C[\"Initialize launch bookkeeping&lt;br/&gt;blocks_dispatched and blocks_done\"]\n    C --&gt; D{\"Any core leaving reset and blocks remain?\"}\n\n    D -- yes --&gt; E[\"Assign next block_id to that core\"]\n    E --&gt; F{\"Is this the final partial block?\"}\n    F -- no --&gt; G[\"core_thread_count = THREADS_PER_BLOCK\"]\n    F -- yes --&gt; H[\"core_thread_count = remaining threads\"]\n    G --&gt; I[\"Raise core_start and increment blocks_dispatched\"]\n    H --&gt; I\n    I --&gt; J{\"Any started core reports core_done?\"}\n\n    D -- no --&gt; J\n    J -- yes --&gt; K[\"Assert core_reset for that core&lt;br/&gt;increment blocks_done\"]\n    K --&gt; L{\"blocks_done == total_blocks?\"}\n    J -- no --&gt; L\n\n    L -- no --&gt; D\n    L -- yes --&gt; M[\"Raise global done\"]</code></pre>"},{"location":"modules/dispatch/#behavior-walkthrough","title":"Behavior walkthrough","text":"<ol> <li>It computes <code>total_blocks = ceil(thread_count / THREADS_PER_BLOCK)</code>.</li> <li>It tracks two counters:</li> <li><code>blocks_dispatched</code></li> <li><code>blocks_done</code></li> <li>When a core is available, the dispatcher gives it the next <code>block_id</code>.</li> <li>For most blocks, <code>core_thread_count</code> equals <code>THREADS_PER_BLOCK</code>.</li> <li>For the final block, <code>core_thread_count</code> may be smaller if the thread count does not divide evenly.</li> <li>When a core reports <code>core_done</code>, the dispatcher resets that core so it can receive more work.</li> <li>When <code>blocks_done == total_blocks</code>, it raises global <code>done</code>.</li> </ol>"},{"location":"modules/dispatch/#control-idea-to-focus-on","title":"Control idea to focus on","text":"<p>This file is not a deep per-cycle datapath. It is a global work-accounting module.</p> <p>The two core questions it answers are:</p> <ul> <li>How many blocks exist?</li> <li>Which core should get the next block?</li> </ul>"},{"location":"modules/dispatch/#timing-notes","title":"Timing notes","text":"<ul> <li><code>start_execution</code> is a small helper flag used to treat level-sensitive <code>start</code> more like a one-time launch event</li> <li><code>core_reset</code> is used both after global reset and between completed blocks</li> <li>A partially full last block matters because the core still has full physical resources, but some thread lanes must stay disabled</li> </ul>"},{"location":"modules/dispatch/#common-pitfalls","title":"Common pitfalls","text":"<ul> <li>Confusing <code>thread_count</code> with <code>THREADS_PER_BLOCK</code></li> <li>Forgetting the final block can be smaller</li> <li>Thinking one core equals one thread; one core actually processes one block at a time</li> </ul>"},{"location":"modules/dispatch/#trace-it-yourself","title":"Trace-it-yourself","text":"<p>If <code>THREADS_PER_BLOCK = 4</code> and <code>thread_count = 10</code>:</p> <ul> <li><code>total_blocks = 3</code></li> <li>block 0 has 4 threads</li> <li>block 1 has 4 threads</li> <li>block 2 has 2 threads</li> </ul> <p>That last value is what eventually becomes <code>core_thread_count</code> for the final issued block.</p>"},{"location":"modules/dispatch/#read-next","title":"Read next","text":"<ul> <li><code>dcr.md</code></li> <li><code>scheduler.md</code></li> <li><code>registers.md</code></li> </ul>"},{"location":"modules/fetcher/","title":"Fetcher Module","text":"<p>Source: <code>src/fetcher.sv</code></p>"},{"location":"modules/fetcher/#what-this-module-is","title":"What this module is","text":"<p><code>fetcher.sv</code> fetches the next instruction from program memory. Each core has one fetcher because all active threads in a core share the same current instruction stream.</p> <p>In DeepWiki's execution model, this is the module responsible for the <code>FETCH</code> stage of the core lifecycle.</p>"},{"location":"modules/fetcher/#where-it-sits-in-tiny-gpu","title":"Where it sits in tiny-gpu","text":"<ul> <li>Upstream: <code>scheduler.sv</code> provides <code>core_state</code> and <code>current_pc</code></li> <li>Downstream: <code>decoder.sv</code> consumes <code>instruction</code></li> <li>Memory path: program memory request goes through the program-memory controller</li> </ul>"},{"location":"modules/fetcher/#clockreset-and-when-work-happens","title":"Clock/reset and when work happens","text":"<ul> <li>Synchronous on <code>posedge clk</code></li> <li>Reset returns the fetcher to <code>IDLE</code></li> <li>It launches a read only during the core's <code>FETCH</code> stage</li> </ul>"},{"location":"modules/fetcher/#interface-cheat-sheet","title":"Interface cheat sheet","text":"Group Meaning <code>core_state</code>, <code>current_pc</code> scheduler tells the fetcher what stage it is in and what address to fetch <code>mem_read_valid</code>, <code>mem_read_address</code> outgoing instruction-memory request <code>mem_read_ready</code>, <code>mem_read_data</code> memory/controller response <code>fetcher_state</code> local FSM state <code>instruction</code> latched fetched instruction"},{"location":"modules/fetcher/#diagram","title":"Diagram","text":"<pre><code>flowchart TD\n    A[\"Fetcher is idle\"] --&gt; B{\"core_state == FETCH?\"}\n    B -- no --&gt; A\n    B -- yes --&gt; C[\"Raise mem_read_valid&lt;br/&gt;present current_pc as mem_read_address\"]\n    C --&gt; D{\"mem_read_ready?\"}\n    D -- no --&gt; C\n    D -- yes --&gt; E[\"Latch mem_read_data into instruction&lt;br/&gt;set fetcher_state to FETCHED\"]\n    E --&gt; F{\"core_state == DECODE?\"}\n    F -- no --&gt; E\n    F -- yes --&gt; G[\"Return to IDLE for next fetch\"]</code></pre>"},{"location":"modules/fetcher/#behavior-walkthrough","title":"Behavior walkthrough","text":"<ol> <li>While idle, the fetcher watches for <code>core_state == FETCH</code>.</li> <li>When that happens, it raises <code>mem_read_valid</code> and presents <code>current_pc</code> as the address.</li> <li>It waits for program memory to respond.</li> <li>When <code>mem_read_ready</code> arrives, it latches <code>mem_read_data</code> into <code>instruction</code>.</li> <li>It then waits for the core to move into <code>DECODE</code>, which marks that this fetch cycle is complete.</li> </ol>"},{"location":"modules/fetcher/#state-machine-idea","title":"State machine idea","text":"<ul> <li><code>IDLE</code>: waiting for a new fetch request</li> <li><code>FETCHING</code>: request is active, waiting for instruction return</li> <li><code>FETCHED</code>: instruction has been captured and is ready for decode</li> </ul>"},{"location":"modules/fetcher/#timing-notes","title":"Timing notes","text":"<ul> <li><code>instruction</code> is stored in a register, so the decoder reads a stable value next stage</li> <li>The fetcher and scheduler are coordinated by <code>core_state</code></li> <li>This design deliberately keeps fetching simple: one instruction at a time, no instruction cache here</li> </ul>"},{"location":"modules/fetcher/#common-pitfalls","title":"Common pitfalls","text":"<ul> <li>Thinking <code>mem_read_ready</code> means \"memory is idle.\" Here it means the fetch completed.</li> <li>Forgetting that the fetcher waits for <code>DECODE</code> before resetting its own state.</li> <li>Confusing data memory and program memory paths; this module uses only program memory.</li> </ul>"},{"location":"modules/fetcher/#trace-it-yourself","title":"Trace-it-yourself","text":"<p>Suppose <code>current_pc = 9</code>:</p> <ol> <li>Scheduler enters <code>FETCH</code></li> <li>Fetcher outputs <code>mem_read_valid = 1</code>, <code>mem_read_address = 9</code></li> <li>Later <code>mem_read_ready = 1</code> and the instruction word returns</li> <li>Fetcher stores that instruction and moves to <code>FETCHED</code></li> <li>Once scheduler enters <code>DECODE</code>, fetcher returns to <code>IDLE</code></li> </ol>"},{"location":"modules/fetcher/#read-next","title":"Read next","text":"<ul> <li><code>decoder.md</code></li> <li><code>scheduler.md</code></li> <li><code>controller.md</code></li> </ul>"},{"location":"modules/gpu/","title":"GPU Module","text":"<p>Source: <code>src/gpu.sv</code></p>"},{"location":"modules/gpu/#what-this-module-is","title":"What this module is","text":"<p><code>gpu.sv</code> is the top-level integration module for the whole design. It does not mainly perform arithmetic or decode instructions itself. Instead, it connects all major subsystems into one complete GPU.</p> <p>The best beginner mental model is:</p> <p><code>gpu.sv</code> is the top-level wiring and orchestration shell.</p> <p>It ties together:</p> <ul> <li>launch configuration (<code>dcr</code>)</li> <li>block assignment (<code>dispatch</code>)</li> <li>memory arbitration (<code>controller</code>)</li> <li>actual execution (<code>core</code> instances)</li> </ul> <p>This matches the DeepWiki architecture pages, where the GPU module is described as the wrapper around cores, memory controllers, and dispatch logic.</p>"},{"location":"modules/gpu/#where-it-sits-in-tiny-gpu","title":"Where it sits in tiny-gpu","text":"<ul> <li>Upstream: host/testbench drives <code>start</code>, <code>device_control_write_enable</code>, and the external memory interfaces</li> <li>Inside the GPU:</li> <li>one <code>dcr</code></li> <li>one data-memory controller</li> <li>one program-memory controller</li> <li>one <code>dispatch</code></li> <li><code>NUM_CORES</code> compute cores</li> <li>Downstream: external data/program memory ports and the host-visible <code>done</code></li> </ul>"},{"location":"modules/gpu/#clockreset-and-when-work-happens","title":"Clock/reset and when work happens","text":"<ul> <li>Entire design is clocked by <code>clk</code></li> <li><code>reset</code> resets the top-level submodules and their children</li> <li>Execution begins only after:</li> <li>the host loads memory</li> <li>the host writes thread count into the DCR</li> <li>the host asserts <code>start</code></li> </ul>"},{"location":"modules/gpu/#interface-cheat-sheet","title":"Interface cheat sheet","text":"Group Meaning <code>start</code>, <code>done</code> top-level kernel launch and completion <code>device_control_*</code> host writes launch metadata into the DCR <code>program_mem_*</code> external program memory interface <code>data_mem_*</code> external data memory interface <code>core_*</code> arrays dispatcher-managed per-core launch/status signals <code>lsu_*</code> arrays flattened all-core/all-thread data-memory traffic <code>fetcher_*</code> arrays per-core instruction-fetch traffic"},{"location":"modules/gpu/#diagram","title":"Diagram","text":"<pre><code>flowchart TD\n    HOST[\"Host or testbench\"] --&gt; CFG[\"Write thread_count into dcr\"]\n    CFG --&gt; DCR[\"dcr\"]\n    HOST --&gt; START[\"start pulse\"]\n\n    DCR --&gt; DISP[\"dispatch\"]\n    START --&gt; DISP\n    DISP --&gt; CORE0[\"core 0\"]\n    DISP --&gt; CORE1[\"core 1 .. core N\"]\n    CORE0 --&gt; FE0[\"fetcher traffic\"]\n    CORE1 --&gt; FE1[\"fetcher traffic\"]\n    FE0 --&gt; PMCTRL[\"program memory controller\"]\n    FE1 --&gt; PMCTRL\n    PMCTRL --&gt; PMEM[\"external program memory\"]\n\n    CORE0 --&gt; LSU0[\"lane LSU traffic\"]\n    CORE1 --&gt; LSU1[\"lane LSU traffic\"]\n    LSU0 --&gt; FLAT[\"flatten all core and lane LSU ports\"]\n    LSU1 --&gt; FLAT\n    FLAT --&gt; DMCTRL[\"data memory controller\"]\n    DMCTRL --&gt; DMEM[\"external data memory\"]\n\n    CORE0 --&gt; DONE0[\"core_done\"]\n    CORE1 --&gt; DONE1[\"core_done\"]\n    DONE0 --&gt; DISP\n    DONE1 --&gt; DISP\n    DISP --&gt; DONE[\"global done\"]</code></pre>"},{"location":"modules/gpu/#how-to-read-this-file","title":"How to read this file","text":"<p>This file is mostly about connectivity, not local algorithms.</p> <p>When reading it, ask these questions:</p> <ol> <li>Which modules exist once at GPU scope?</li> <li>Which modules are instantiated once per core?</li> <li>How are per-core and per-thread signals flattened so the memory controllers can see them?</li> <li>How does the launch flow move from host -&gt; DCR -&gt; dispatch -&gt; core?</li> </ol>"},{"location":"modules/gpu/#behavior-walkthrough","title":"Behavior walkthrough","text":"<ol> <li>The host writes <code>thread_count</code> into <code>dcr</code>.</li> <li>The host asserts <code>start</code>.</li> <li><code>dispatch</code> uses <code>thread_count</code> to break the kernel into blocks and assign those blocks to cores.</li> <li>Each core runs one block at a time.</li> <li>Core fetchers request instructions through the program-memory controller.</li> <li>Core LSUs request data-memory reads/writes through the data-memory controller.</li> <li>As cores finish blocks, <code>dispatch</code> either assigns new blocks or eventually raises global <code>done</code>.</li> </ol>"},{"location":"modules/gpu/#the-most-important-structural-idea-flattening","title":"The most important structural idea: flattening","text":"<p>Inside a core, LSU traffic is naturally grouped as:</p> <ul> <li>per core</li> <li>per thread lane</li> </ul> <p>But the controller wants one flat list of consumers.</p> <p>So <code>gpu.sv</code> creates flattened arrays such as:</p> <ul> <li><code>lsu_read_valid[NUM_LSUS-1:0]</code></li> <li><code>lsu_write_valid[NUM_LSUS-1:0]</code></li> </ul> <p>where:</p> <pre><code>NUM_LSUS = NUM_CORES * THREADS_PER_BLOCK\n</code></pre> <p>Then it bridges each core-local lane into a unique global LSU index:</p> <pre><code>lsu_index = i * THREADS_PER_BLOCK + j\n</code></pre> <p>That indexing rule is one of the most important things to understand in this file.</p>"},{"location":"modules/gpu/#one-instance-modules-vs-replicated-modules","title":"One-instance modules vs replicated modules","text":""},{"location":"modules/gpu/#single-gpu-wide-instances","title":"Single GPU-wide instances","text":"<ul> <li><code>dcr</code></li> <li><code>dispatch</code></li> <li>one data-memory controller</li> <li>one program-memory controller</li> </ul> <p>These exist once because they coordinate global behavior.</p>"},{"location":"modules/gpu/#repeated-per-core-instances","title":"Repeated per-core instances","text":"<p>Inside the <code>generate</code> loop, <code>gpu.sv</code> instantiates one <code>core</code> per <code>i</code>.</p> <p>So if <code>NUM_CORES = 2</code>, the generated hardware contains two compute cores.</p>"},{"location":"modules/gpu/#the-bridging-logic","title":"The bridging logic","text":"<p>Inside the nested <code>generate</code> structure, the file creates per-core local LSU wires like:</p> <ul> <li><code>core_lsu_read_valid</code></li> <li><code>core_lsu_read_address</code></li> </ul> <p>Then it copies them into the flattened global arrays seen by the controller.</p> <p>This is integration glue. It is not new GPU behavior by itself, but it is essential for wiring the hierarchical design together.</p> <p>The comments mention OpenLane / Verilog-2005 compatibility. That tells you this structure is partly shaped by tool constraints, not just by pure architectural elegance.</p>"},{"location":"modules/gpu/#the-full-launch-path","title":"The full launch path","text":"<p>The top-level flow is:</p> <ol> <li>host writes launch metadata into <code>dcr</code></li> <li><code>dcr</code> exposes <code>thread_count</code></li> <li><code>dispatch</code> turns <code>thread_count</code> into blocks and per-core start/reset signals</li> <li>each <code>core</code> executes its assigned block</li> <li>fetch and memory access are arbitrated by the controllers</li> <li>once all blocks finish, <code>dispatch</code> raises <code>done</code></li> </ol> <p>This is the main reason <code>gpu.sv</code> feels more like a system diagram than a small algorithmic module.</p>"},{"location":"modules/gpu/#timing-notes","title":"Timing notes","text":"<ul> <li>Memory requests do not go directly from cores to external memory; they pass through controllers</li> <li><code>core_done[i]</code> feeds back into dispatch so new blocks can be issued dynamically</li> <li><code>start</code> is consumed by <code>dispatch</code>, not broadcast directly as full execution control into every submodule</li> </ul>"},{"location":"modules/gpu/#common-pitfalls","title":"Common pitfalls","text":"<ul> <li>Expecting <code>gpu.sv</code> to contain the execution logic itself. Most real behavior lives in submodules.</li> <li>Getting lost in the many arrays without first separating them into:</li> <li>host interface</li> <li>per-core control</li> <li>flattened fetch traffic</li> <li>flattened LSU traffic</li> <li>Forgetting that <code>gpu.sv</code> must bridge hierarchical structure into controller-friendly flat arrays.</li> <li>Confusing per-core repetition with per-thread repetition. <code>gpu.sv</code> duplicates cores; <code>core.sv</code> duplicates thread lanes.</li> </ul>"},{"location":"modules/gpu/#trace-it-yourself","title":"Trace-it-yourself","text":"<p>Suppose <code>NUM_CORES = 2</code> and <code>THREADS_PER_BLOCK = 4</code>:</p> <ul> <li>there are 2 core instances</li> <li>there are <code>2 * 4 = 8</code> flattened LSU consumer slots</li> <li>one core's lane 0 might map to flattened LSU index 0</li> <li>the other core's lane 0 might map to flattened LSU index 4</li> </ul> <p>This is how one shared data-memory controller can serve all thread-local LSUs across all cores.</p>"},{"location":"modules/gpu/#read-next","title":"Read next","text":"<ul> <li><code>core.md</code></li> <li><code>dispatch.md</code></li> <li><code>controller.md</code></li> </ul>"},{"location":"modules/lsu/","title":"LSU Module","text":"<p>Source: <code>src/lsu.sv</code></p>"},{"location":"modules/lsu/#what-this-module-is","title":"What this module is","text":"<p><code>lsu.sv</code> is the per-thread Load/Store Unit. Each active thread lane has its own LSU so memory accesses can be tracked independently.</p> <p>This is one of the clearest places to learn why the scheduler has a <code>WAIT</code> stage: memory requests take longer than simple arithmetic.</p>"},{"location":"modules/lsu/#where-it-sits-in-tiny-gpu","title":"Where it sits in tiny-gpu","text":"<ul> <li>Upstream: <code>decoder.sv</code> says whether the instruction is <code>LDR</code> or <code>STR</code>; <code>registers.sv</code> supplies <code>rs</code> and <code>rt</code></li> <li>Downstream: data memory controller receives the request; <code>registers.sv</code> may later write back <code>lsu_out</code></li> </ul>"},{"location":"modules/lsu/#clockreset-and-when-work-happens","title":"Clock/reset and when work happens","text":"<ul> <li>Synchronous on <code>posedge clk</code></li> <li>Reset clears request state and handshake outputs</li> <li>Memory requests are launched starting around the core's <code>REQUEST</code> stage and completed before leaving <code>WAIT</code></li> </ul>"},{"location":"modules/lsu/#interface-cheat-sheet","title":"Interface cheat sheet","text":"Group Meaning <code>decoded_mem_read_enable</code>, <code>decoded_mem_write_enable</code> select LDR vs STR behavior <code>rs</code> memory address <code>rt</code> store data for STR <code>mem_read_*</code>, <code>mem_write_*</code> external memory handshake <code>lsu_state</code> local FSM state <code>lsu_out</code> loaded data for later writeback"},{"location":"modules/lsu/#diagram","title":"Diagram","text":"<pre><code>stateDiagram-v2\n    [*] --&gt; IDLE\n    IDLE --&gt; REQUESTING: core_state == REQUEST&lt;br/&gt;and memory op is enabled\n\n    REQUESTING --&gt; WAITING: LDR path&lt;br/&gt;raise mem_read_valid&lt;br/&gt;drive read address from rs\n    REQUESTING --&gt; WAITING: STR path&lt;br/&gt;raise mem_write_valid&lt;br/&gt;drive write address from rs&lt;br/&gt;drive write data from rt\n\n    WAITING --&gt; WAITING: wait for memory-side ready\n    WAITING --&gt; DONE: load completes&lt;br/&gt;capture mem_read_data into lsu_out\n    WAITING --&gt; DONE: store completes&lt;br/&gt;write acknowledged\n\n    DONE --&gt; DONE: wait for core UPDATE stage\n    DONE --&gt; IDLE: core_state == UPDATE</code></pre>"},{"location":"modules/lsu/#behavior-walkthrough","title":"Behavior walkthrough","text":"<ol> <li>The decoder chooses whether this instruction is a load or store.</li> <li>In <code>REQUEST</code>, the LSU begins the transaction.</li> <li>In <code>REQUESTING</code>, it drives either:</li> <li>read address (<code>LDR</code>)</li> <li>write address + write data (<code>STR</code>)</li> <li>In <code>WAITING</code>, it waits for the controller/memory to acknowledge completion.</li> <li>For a load, it captures returned data into <code>lsu_out</code>.</li> <li>In <code>DONE</code>, it waits until <code>UPDATE</code> before resetting back to <code>IDLE</code>.</li> </ol>"},{"location":"modules/lsu/#state-machine-idea","title":"State machine idea","text":"<ul> <li><code>IDLE</code>: no memory op in flight</li> <li><code>REQUESTING</code>: presenting a fresh request</li> <li><code>WAITING</code>: request has been sent, waiting for completion</li> <li><code>DONE</code>: completion reached, waiting for the core's per-instruction cleanup point</li> </ul> <p>The same FSM is reused for both loads and stores; only the handshake signals differ.</p>"},{"location":"modules/lsu/#timing-notes","title":"Timing notes","text":"<ul> <li>This module is the reason the scheduler must sometimes stall in <code>WAIT</code></li> <li><code>lsu_out</code> is only meaningful after a load has completed</li> <li>Returning to <code>IDLE</code> in <code>UPDATE</code> keeps the per-instruction rhythm aligned with the rest of the core</li> </ul>"},{"location":"modules/lsu/#common-pitfalls","title":"Common pitfalls","text":"<ul> <li>Thinking memory requests finish in one cycle like ALU operations</li> <li>Forgetting that <code>rs</code> is used as the memory address in this design</li> <li>Missing that <code>lsu_out</code> is only for <code>LDR</code>, not <code>STR</code></li> </ul>"},{"location":"modules/lsu/#trace-it-yourself","title":"Trace-it-yourself","text":"<p>For <code>LDR R4, R4</code>:</p> <ol> <li>Register file has already copied <code>R4</code> into <code>rs</code></li> <li>LSU enters <code>REQUESTING</code> and drives <code>mem_read_address = rs</code></li> <li>It waits in <code>WAITING</code></li> <li>When <code>mem_read_ready</code> is asserted, it stores <code>mem_read_data</code> into <code>lsu_out</code></li> <li>In <code>UPDATE</code>, the register file writes <code>lsu_out</code> into <code>R4</code></li> </ol>"},{"location":"modules/lsu/#read-next","title":"Read next","text":"<ul> <li><code>controller.md</code></li> <li><code>scheduler.md</code></li> <li><code>registers.md</code></li> </ul>"},{"location":"modules/pc/","title":"PC Module","text":"<p>Source: <code>src/pc.sv</code></p>"},{"location":"modules/pc/#what-this-module-is","title":"What this module is","text":"<p><code>pc.sv</code> computes the next program counter for one thread lane and stores that lane's NZP condition state.</p> <p>This module is where the tiny-gpu branch mechanism comes together:</p> <ul> <li><code>decoder.sv</code> says whether the instruction is a branch</li> <li><code>alu.sv</code> computes compare flags for <code>CMP</code></li> <li><code>pc.sv</code> decides whether to take the branch and where to go next</li> </ul>"},{"location":"modules/pc/#where-it-sits-in-tiny-gpu","title":"Where it sits in tiny-gpu","text":"<ul> <li>Upstream: decoder provides <code>decoded_pc_mux</code>, <code>decoded_nzp</code>, <code>decoded_immediate</code>, and <code>decoded_nzp_write_enable</code>; ALU provides <code>alu_out</code></li> <li>Downstream: scheduler later chooses one representative <code>next_pc</code> value as the core's shared <code>current_pc</code></li> </ul>"},{"location":"modules/pc/#clockreset-and-when-work-happens","title":"Clock/reset and when work happens","text":"<ul> <li>Synchronous on <code>posedge clk</code></li> <li>Reset clears both <code>nzp</code> and <code>next_pc</code></li> <li>Two important stages:</li> <li><code>EXECUTE</code>: compute <code>next_pc</code></li> <li><code>UPDATE</code>: refresh <code>nzp</code> from compare output if needed</li> </ul>"},{"location":"modules/pc/#interface-cheat-sheet","title":"Interface cheat sheet","text":"Group Meaning <code>current_pc</code> current converged PC from the scheduler <code>decoded_pc_mux</code> whether to use branch logic or plain PC+1 <code>decoded_nzp</code>, <code>decoded_immediate</code> branch condition mask and branch target <code>decoded_nzp_write_enable</code> whether this instruction updates NZP <code>alu_out</code> low 3 bits carry compare results for CMP <code>next_pc</code> this thread lane's computed next PC"},{"location":"modules/pc/#diagram","title":"Diagram","text":"<pre><code>flowchart TD\n    subgraph ExecuteStage[\"EXECUTE stage: choose next PC\"]\n        A[\"Start from current_pc\"] --&gt; B{\"decoded_pc_mux?\"}\n        B -- no --&gt; C[\"Sequential flow&lt;br/&gt;next_pc = current_pc + 1\"]\n        B -- yes --&gt; D{\"Stored NZP matches decoded branch mask?\"}\n        D -- yes --&gt; E[\"Branch taken&lt;br/&gt;next_pc = decoded_immediate\"]\n        D -- no --&gt; C\n    end\n\n    subgraph UpdateStage[\"UPDATE stage: refresh NZP state\"]\n        F{\"decoded_nzp_write_enable?\"}\n        F -- yes --&gt; G[\"Store compare-result bits from ALU low bits\"]\n        F -- no --&gt; H[\"Keep previous NZP state\"]\n    end</code></pre>"},{"location":"modules/pc/#behavior-walkthrough","title":"Behavior walkthrough","text":"<ol> <li>During <code>EXECUTE</code>, the PC logic decides what the next instruction address should be.</li> <li>If this is not a branch instruction, it simply does <code>current_pc + 1</code>.</li> <li>If this is <code>BRnzp</code>, it checks whether the stored <code>nzp</code> bits match the branch mask.</li> <li>If they match, it jumps to <code>decoded_immediate</code>.</li> <li>Separately, in <code>UPDATE</code>, a previous <code>CMP</code> can refresh the stored <code>nzp</code> bits from <code>alu_out[2:0]</code>.</li> </ol>"},{"location":"modules/pc/#decision-logic-to-focus-on","title":"Decision logic to focus on","text":"<ul> <li>Branch selection is based on <code>decoded_pc_mux</code></li> <li>Branch condition is based on <code>(nzp &amp; decoded_nzp) != 0</code></li> <li>NZP update happens later than compare execution</li> </ul> <p>That last point is crucial: compare result generation and NZP state update are not the same moment.</p>"},{"location":"modules/pc/#timing-notes","title":"Timing notes","text":"<ul> <li><code>CMP</code> produces the bits in the ALU during <code>EXECUTE</code></li> <li><code>pc.sv</code> stores those bits into <code>nzp</code> during <code>UPDATE</code></li> <li>A future branch reads the stored <code>nzp</code> state</li> </ul>"},{"location":"modules/pc/#common-pitfalls","title":"Common pitfalls","text":"<ul> <li>Thinking the immediate is always used as the next PC. It is only used when a branch is taken.</li> <li>Forgetting that <code>nzp</code> is stateful and persists across instructions.</li> <li>Missing the repo's simplifying assumption: each thread lane computes a <code>next_pc</code>, but the scheduler later assumes all lanes converge.</li> </ul>"},{"location":"modules/pc/#trace-it-yourself","title":"Trace-it-yourself","text":"<p>Imagine <code>CMP R9, R2</code> followed by <code>BRn LOOP</code>:</p> <ol> <li><code>CMP</code> causes ALU to pack gt/eq/lt bits into <code>alu_out[2:0]</code></li> <li>In <code>UPDATE</code>, PC module stores those bits into <code>nzp</code></li> <li>On the later branch instruction, <code>decoded_pc_mux = 1</code></li> <li>If the negative bit matches the branch mask, <code>next_pc</code> becomes the loop label address</li> </ol>"},{"location":"modules/pc/#read-next","title":"Read next","text":"<ul> <li><code>alu.md</code></li> <li><code>decoder.md</code></li> <li><code>scheduler.md</code></li> </ul>"},{"location":"modules/registers/","title":"Registers Module","text":"<p>Source: <code>src/registers.sv</code></p>"},{"location":"modules/registers/#what-this-module-is","title":"What this module is","text":"<p><code>registers.sv</code> is the per-thread register file. Each thread lane gets its own private set of 16 registers, which is one of the main reasons SIMD execution works: all threads execute the same instruction, but each thread reads and writes its own data.</p> <p>DeepWiki's thread-unit description highlights the special metadata registers here. They are especially important for understanding <code>blockIdx * blockDim + threadIdx</code>.</p>"},{"location":"modules/registers/#where-it-sits-in-tiny-gpu","title":"Where it sits in tiny-gpu","text":"<ul> <li>Upstream: decoder provides register addresses and writeback controls; ALU/LSU/immediate paths provide candidate writeback data</li> <li>Downstream: ALU and LSU consume <code>rs</code> and <code>rt</code></li> </ul>"},{"location":"modules/registers/#clockreset-and-when-work-happens","title":"Clock/reset and when work happens","text":"<ul> <li>Synchronous on <code>posedge clk</code></li> <li>Reset clears general registers and initializes special registers</li> <li>Important stages:</li> <li><code>REQUEST</code>: read source operands into <code>rs</code> and <code>rt</code></li> <li><code>UPDATE</code>: write result into <code>rd</code></li> </ul>"},{"location":"modules/registers/#interface-cheat-sheet","title":"Interface cheat sheet","text":"Group Meaning <code>decoded_rd/rs/rt_address</code> which registers to write/read <code>decoded_reg_write_enable</code> whether UPDATE should write into <code>rd</code> <code>decoded_reg_input_mux</code> choose ALU result, memory result, or immediate <code>alu_out</code>, <code>lsu_out</code>, <code>decoded_immediate</code> writeback sources <code>block_id</code> current block index from dispatch/core <code>rs</code>, <code>rt</code> source operand outputs for this thread lane"},{"location":"modules/registers/#diagram","title":"Diagram","text":"<pre><code>flowchart TD\n    A[\"Clock edge arrives\"] --&gt; B{\"reset?\"}\n    B -- yes --&gt; C[\"Clear R0-R12&lt;br/&gt;initialize R13-R15 metadata registers\"]\n\n    B -- no --&gt; D{\"enable?\"}\n    D -- no --&gt; E[\"Hold register file state\"]\n    D -- yes --&gt; F[\"Refresh R13 from current block_id\"]\n\n    F --&gt; G{\"core_state == REQUEST?\"}\n    G -- yes --&gt; H[\"Snapshot source operands&lt;br/&gt;rs &lt;- selected source register&lt;br/&gt;rt &lt;- selected target register\"]\n    G -- no --&gt; I[\"Do not update rs/rt this cycle\"]\n\n    H --&gt; J{\"core_state == UPDATE and write enabled and rd is a general register?\"}\n    I --&gt; J\n\n    J -- no --&gt; K[\"No writeback this cycle\"]\n    J -- yes --&gt; L{\"decoded_reg_input_mux\"}\n    L -- ARITHMETIC --&gt; M[\"Write alu_out into rd\"]\n    L -- MEMORY --&gt; N[\"Write lsu_out into rd\"]\n    L -- CONSTANT --&gt; O[\"Write immediate into rd\"]</code></pre>"},{"location":"modules/registers/#behavior-walkthrough","title":"Behavior walkthrough","text":"<ol> <li>On reset, it clears the general-purpose registers.</li> <li>It initializes:</li> <li><code>R13 = %blockIdx</code></li> <li><code>R14 = %blockDim</code></li> <li><code>R15 = %threadIdx</code></li> <li>While enabled, it keeps <code>R13</code> synchronized with the current <code>block_id</code>.</li> <li>In <code>REQUEST</code>, it snapshots the source registers named by the current instruction into <code>rs</code> and <code>rt</code>.</li> <li>In <code>UPDATE</code>, if writeback is enabled and <code>rd &lt; 13</code>, it writes one of three sources into <code>rd</code>.</li> </ol>"},{"location":"modules/registers/#decision-logic-to-focus-on","title":"Decision logic to focus on","text":"<ul> <li>Source read timing is stage-based, not asynchronous</li> <li>Writeback uses a 3-way mux</li> <li>Special metadata registers are protected by <code>decoded_rd_address &lt; 13</code></li> </ul>"},{"location":"modules/registers/#timing-notes","title":"Timing notes","text":"<ul> <li><code>rs</code> and <code>rt</code> are registers themselves, not direct array aliases</li> <li>The module reads in <code>REQUEST</code> and writes in <code>UPDATE</code></li> <li>That staged rhythm is what keeps operand use and result commit aligned with the rest of the core</li> </ul>"},{"location":"modules/registers/#common-pitfalls","title":"Common pitfalls","text":"<ul> <li>Thinking <code>R13-R15</code> are normal writable registers. They are treated as read-only metadata.</li> <li>Forgetting that <code>%blockIdx</code> is refreshed from <code>block_id</code>.</li> <li>Missing that the source operands are copied out into <code>rs</code> and <code>rt</code> before ALU/LSU use.</li> </ul>"},{"location":"modules/registers/#trace-it-yourself","title":"Trace-it-yourself","text":"<p>For <code>CONST R2, #8</code>:</p> <ol> <li>Decoder sets <code>decoded_reg_write_enable = 1</code></li> <li>Decoder sets <code>decoded_reg_input_mux = CONSTANT</code></li> <li>In <code>UPDATE</code>, register file writes <code>decoded_immediate</code> into <code>R2</code></li> </ol> <p>For <code>ADD R6, R4, R5</code>:</p> <ol> <li>In <code>REQUEST</code>, it copies <code>R4 -&gt; rs</code>, <code>R5 -&gt; rt</code></li> <li>ALU uses those values in <code>EXECUTE</code></li> <li>In <code>UPDATE</code>, it writes <code>alu_out</code> into <code>R6</code></li> </ol>"},{"location":"modules/registers/#read-next","title":"Read next","text":"<ul> <li><code>alu.md</code></li> <li><code>lsu.md</code></li> <li><code>pc.md</code></li> </ul>"},{"location":"modules/scheduler/","title":"Scheduler Module","text":"<p>Source: <code>src/scheduler.sv</code></p>"},{"location":"modules/scheduler/#what-this-module-is","title":"What this module is","text":"<p><code>scheduler.sv</code> is the core's master stage machine. If you want one file that explains the overall execution rhythm of a block, this is the file.</p> <p>DeepWiki's execution-model page describes the six-stage flow <code>FETCH -&gt; DECODE -&gt; REQUEST -&gt; WAIT -&gt; EXECUTE -&gt; UPDATE</code>. This module is exactly where that flow is enforced.</p>"},{"location":"modules/scheduler/#where-it-sits-in-tiny-gpu","title":"Where it sits in tiny-gpu","text":"<ul> <li>Upstream: <code>dispatch.sv</code> provides <code>start</code>; fetcher and LSUs report progress</li> <li>Downstream: all other core-local modules react to <code>core_state</code></li> <li>Key idea: <code>core_state</code> is the shared control clocking rhythm for the whole core</li> </ul>"},{"location":"modules/scheduler/#clockreset-and-when-work-happens","title":"Clock/reset and when work happens","text":"<ul> <li>Synchronous on <code>posedge clk</code></li> <li>Reset sets <code>current_pc = 0</code>, <code>core_state = IDLE</code>, <code>done = 0</code></li> <li>Every instruction executed by a block passes through the same state sequence</li> </ul>"},{"location":"modules/scheduler/#interface-cheat-sheet","title":"Interface cheat sheet","text":"Group Meaning <code>start</code> begin processing this block <code>fetcher_state</code> tells scheduler when instruction fetch completed <code>lsu_state[]</code> tells scheduler whether any thread is still waiting on memory <code>decoded_ret</code> end-of-kernel/block instruction marker <code>next_pc[]</code> each thread lane's computed next PC <code>core_state</code> shared stage broadcast to the core <code>current_pc</code> the core's converged PC <code>done</code> this block has finished executing"},{"location":"modules/scheduler/#diagram","title":"Diagram","text":"<pre><code>stateDiagram-v2\n    [*] --&gt; IDLE\n    IDLE --&gt; FETCH: start\n    FETCH --&gt; DECODE: fetcher_state == FETCHED\n    DECODE --&gt; REQUEST\n    REQUEST --&gt; WAIT\n    WAIT --&gt; WAIT: any LSU still REQUESTING or WAITING\n    WAIT --&gt; EXECUTE: no LSU still REQUESTING/WAITING\n    EXECUTE --&gt; UPDATE\n    UPDATE --&gt; DONE: decoded_ret\n    UPDATE --&gt; FETCH: otherwise current_pc = one next_pc value\n    DONE --&gt; DONE</code></pre>"},{"location":"modules/scheduler/#behavior-walkthrough","title":"Behavior walkthrough","text":"<ol> <li>In <code>IDLE</code>, the scheduler waits for the core to be started on a new block.</li> <li><code>FETCH</code> waits until the fetcher reports that the instruction has arrived.</li> <li><code>DECODE</code> gives the decoder one cycle to produce control signals.</li> <li><code>REQUEST</code> lets registers/LSUs launch their work.</li> <li><code>WAIT</code> stalls if any LSU still has an in-flight memory operation.</li> <li><code>EXECUTE</code> is where ALUs and PC logic do their main calculations.</li> <li><code>UPDATE</code> commits results and either:</li> <li>ends the block on <code>RET</code></li> <li>or advances to the next instruction</li> </ol>"},{"location":"modules/scheduler/#state-machine-idea","title":"State machine idea","text":"<ul> <li><code>IDLE</code>: no active block</li> <li><code>FETCH</code>: instruction fetch in progress</li> <li><code>DECODE</code>: instruction decode</li> <li><code>REQUEST</code>: operand snapshot / memory request launch</li> <li><code>WAIT</code>: memory-latency hiding point in this simple design</li> <li><code>EXECUTE</code>: perform arithmetic/branch logic</li> <li><code>UPDATE</code>: commit state updates</li> <li><code>DONE</code>: block finished</li> </ul>"},{"location":"modules/scheduler/#timing-notes","title":"Timing notes","text":"<ul> <li><code>WAIT</code> is the key stage for understanding asynchronous memory</li> <li>The scheduler chooses <code>current_pc &lt;= next_pc[THREADS_PER_BLOCK-1]</code> as a representative value, which encodes the repo's simplifying assumption that active threads reconverge to one PC</li> <li><code>done</code> is asserted only when a <code>RET</code> instruction reaches <code>UPDATE</code></li> </ul> <p>The Mermaid diagram intentionally shows a self-loop on <code>WAIT</code> because that stage can last multiple cycles when any thread LSU still has an in-flight memory operation.</p>"},{"location":"modules/scheduler/#common-pitfalls","title":"Common pitfalls","text":"<ul> <li>Thinking every instruction always spends many cycles in <code>WAIT</code>. Non-memory instructions pass through quickly.</li> <li>Missing that <code>decoded_ret</code> is handled in <code>UPDATE</code>, not immediately in <code>DECODE</code>.</li> <li>Forgetting the branch-divergence simplification when reading the <code>next_pc[]</code> array.</li> </ul>"},{"location":"modules/scheduler/#trace-it-yourself","title":"Trace-it-yourself","text":"<p>For a non-memory <code>ADD</code> instruction, the rough rhythm is:</p> <ol> <li><code>FETCH</code> gets the instruction</li> <li><code>DECODE</code> produces arithmetic controls</li> <li><code>REQUEST</code> snapshots source operands</li> <li><code>WAIT</code> exits quickly because no LSU is busy</li> <li><code>EXECUTE</code> computes <code>alu_out</code></li> <li><code>UPDATE</code> writes the result and advances <code>current_pc</code></li> </ol> <p>For <code>LDR</code>, the difference is that <code>WAIT</code> lasts until the matching LSU finishes.</p>"},{"location":"modules/scheduler/#read-next","title":"Read next","text":"<ul> <li><code>decoder.md</code></li> <li><code>fetcher.md</code></li> <li><code>lsu.md</code></li> </ul>"},{"location":"refsim/","title":"C++ reference simulator (<code>opengpu-refsim</code>)","text":"<p>A deterministic golden model of the OpenGPU 16-bit ISA. Used for Catch2 unit tests, the DiffTest harness, and documentation. Source: <code>sim/cppref/</code>.</p>"},{"location":"refsim/#build","title":"Build","text":"<pre><code>cmake -S sim/cppref -B build/cppref -G Ninja\ncmake --build build/cppref\nctest --test-dir build/cppref --output-on-failure\n</code></pre> <p>The toolchain image (<code>ghcr.io/piyushptiwari1/opengpu-toolchain:latest</code>) already ships GCC 13, CMake \u2265 3.28 and Ninja. Catch2 is fetched on first build via CMake <code>FetchContent</code>. <code>OPENGPU_REFSIM_WERROR=ON</code> is the default \u2014 <code>gcc</code> and <code>clang</code> are both clean under the full warning set.</p>"},{"location":"refsim/#run","title":"Run","text":"<pre><code>./build/cppref/opengpu-refsim \\\n    --program program.hex \\\n    --data    data.hex     \\\n    --threads 8            \\\n    --blocks  1            \\\n    --trace   trace.jsonl  \\\n    --max-steps 200\n</code></pre> <p><code>trace.jsonl</code> is line-delimited JSON, one record per retired instruction. This is the format consumed by the DiffTest harness.</p>"},{"location":"refsim/#trace-schema","title":"Trace schema","text":"<p>Each line of <code>trace.jsonl</code> looks like:</p> <pre><code>{\"tick\":0,\"tid\":0,\"pc\":0,\"instr\":20480,\"op\":\"MUL\",\"rd\":0,\n \"rd_val\":0,\"mem_w\":null,\"nzp\":0,\"done\":false}\n</code></pre> Field Type Meaning <code>tick</code> int refsim cycle counter <code>tid</code> int thread id within the kernel grid <code>pc</code> int program counter at retire <code>instr</code> int 16-bit instruction word <code>op</code> string mnemonic (matches ISA YAML) <code>rd</code> int / null destination register, <code>null</code> if no write <code>rd_val</code> int / null value written to <code>rd</code>, <code>null</code> if no write <code>mem_w</code> object / null <code>{\"addr\":A,\"data\":D}</code> for <code>STR</code>, else <code>null</code> <code>nzp</code> int NZP flags after the insn (3 bits) <code>done</code> bool thread asserted <code>done</code> this cycle"},{"location":"refsim/#what-the-tests-cover","title":"What the tests cover","text":"<ul> <li>Decode field layout \u2014 <code>instr[15:12] = opcode</code>, R-type fields,   I-type immediate, B-type NZP + target.</li> <li>ALU edge cases \u2014 DIV-by-zero returns 0, ADD wraps at 8 bits.</li> <li>CMP NZP semantics \u2014 exactly one of N, Z, P is set per result.</li> <li>System registers \u2014 <code>R15</code> reads back per-thread <code>threadIdx</code>.</li> <li>End-to-end matadd-8 \u2014 same program/data as the cocotb   <code>test_matadd.py</code>, asserts every output element matches the   reference sum.</li> </ul>"},{"location":"report/","title":"tiny-gpu Repository Analysis Report","text":"<p>This directory contains a source-verified documentation set for the <code>tiny-gpu</code> repository.</p>"},{"location":"report/#files","title":"Files","text":"<ul> <li><code>00-executive-overview.md</code> \u2014 project purpose, scope, major findings, and external toolchain summary</li> <li><code>01-repository-structure.md</code> \u2014 top-level layout, module inventory, and hierarchy mapping</li> <li><code>02-architecture-and-execution.md</code> \u2014 hardware organization, execution stages, and key module relationships</li> <li><code>03-build-and-test-workflow.md</code> \u2014 compile flow, cocotb harness behavior, and simulator/tooling references</li> <li><code>04-risks-open-questions.md</code> \u2014 confirmed limitations, documentation mismatches, and roadmap boundaries</li> <li><code>05-matadd-trace-walkthrough.md</code> \u2014 cycle-by-cycle guided reading of the matrix-addition execution log</li> <li><code>06-matmul-trace-walkthrough.md</code> \u2014 cycle-by-cycle guided reading of the matrix-multiplication execution log</li> </ul>"},{"location":"report/#method","title":"Method","text":"<p>This report was cross-checked against four sources of evidence:</p> <ol> <li>the local checkout (<code>README.md</code>, <code>Makefile</code>, all RTL modules in <code>src/</code>, and cocotb code in <code>test/</code>)</li> <li>the repository remote (<code>adam-maj/tiny-gpu</code>)</li> <li>DeepWiki repository documentation for <code>adam-maj/tiny-gpu</code></li> <li>official tool documentation for <code>cocotb</code>, <code>iverilog</code>, <code>sv2v</code>, and <code>GTKWave</code></li> </ol>"},{"location":"report/#reading-guide","title":"Reading guide","text":"<ul> <li>Treat <code>src/</code> as the source of truth for implemented behavior.</li> <li>Treat <code>README.md</code> as the conceptual guide and roadmap.</li> <li>Treat external tool references in this report as usage context for the simulation flow, not as proof of repository behavior.</li> </ul>"},{"location":"report/#diagram-note","title":"Diagram note","text":"<p>Mermaid diagrams are included as source fences in the Markdown so the structure remains readable even when diagrams are not rendered by the viewer.</p>"},{"location":"report/00-executive-overview/","title":"Executive Overview","text":""},{"location":"report/00-executive-overview/#what-this-repository-is","title":"What this repository is","text":"<p><code>tiny-gpu</code> is a small educational GPU implementation written in SystemVerilog and exercised through a Python + cocotb simulation harness. The codebase is intentionally compact: the hardware lives entirely in <code>src/</code>, the tests live entirely in <code>test/</code>, and the project uses two example kernels to demonstrate SIMD-style execution on a simplified GPU architecture.</p> <p>The local checkout is connected to <code>https://github.com/adam-maj/tiny-gpu.git</code>. External DeepWiki material was used as a secondary explanatory source, but local source files remain the authority for implementation details.</p>"},{"location":"report/00-executive-overview/#confirmed-headline-findings","title":"Confirmed headline findings","text":"<ul> <li>The hardware source is concentrated in 12 SystemVerilog modules under <code>src/</code>.</li> <li>The top-level implementation is <code>src/gpu.sv</code>, which instantiates the device control register, dispatcher, two memory controllers, and a configurable array of compute cores.</li> <li>Each core contains one <code>fetcher</code>, one <code>decoder</code>, one <code>scheduler</code>, and per-thread <code>alu</code>, <code>lsu</code>, <code>registers</code>, and <code>pc</code> units.</li> <li>The execution model is explicitly staged in the scheduler as <code>FETCH -&gt; DECODE -&gt; REQUEST -&gt; WAIT -&gt; EXECUTE -&gt; UPDATE</code>.</li> <li>The verification story is cocotb-driven: Python code simulates both program memory and data memory while the RTL runs inside Icarus Verilog.</li> <li>Two proof-of-concept kernels are exercised by tests: matrix addition and matrix multiplication.</li> </ul>"},{"location":"report/00-executive-overview/#high-level-architecture","title":"High-level architecture","text":"<pre><code>flowchart TD\n    Host[Host / cocotb testbench] --&gt; DCR[Device Control Register]\n    Host --&gt; PMem[Program Memory Model]\n    Host --&gt; DMem[Data Memory Model]\n    Host --&gt; Start[start]\n\n    DCR --&gt; Dispatch[Dispatcher]\n    Start --&gt; Dispatch\n    Dispatch --&gt; GPU[GPU Top Module]\n    GPU --&gt; Cores[Core Array]\n    Cores --&gt; ProgCtrl[Program Memory Controller]\n    Cores --&gt; DataCtrl[Data Memory Controller]\n    ProgCtrl --&gt; PMem\n    DataCtrl --&gt; DMem</code></pre>"},{"location":"report/00-executive-overview/#toolchain-summary","title":"Toolchain summary","text":"<p>The build and simulation flow depends on four external tools:</p> Tool Role in this repo Grounded evidence Official reference used here <code>cocotb</code> Python testbench framework <code>test/helpers/setup.py</code>, <code>test/test_*.py</code>, <code>Makefile</code> cocotb docs on async tests, clocks, triggers, and DUT signal access <code>iverilog</code> / <code>vvp</code> compiles and runs the Verilog simulation <code>Makefile</code> Icarus Verilog docs for <code>-g2012</code>, <code>-s</code>, and VPI runtime flags <code>sv2v</code> converts SystemVerilog to Verilog before compilation <code>Makefile</code>, <code>README.md</code> sv2v README usage and CLI options <code>gtkwave</code> optional waveform/debug viewer <code>Makefile</code> (<code>show_%</code> rule and TODO only) GTKWave docs for post-mortem waveform inspection"},{"location":"report/00-executive-overview/#important-caveats","title":"Important caveats","text":"<ul> <li>The README discusses a cache as if it were part of the architecture, but there is no cache module in <code>src/</code> and no cache instantiated in <code>src/gpu.sv</code>.</li> <li>The build flow assumes a pre-existing <code>build/</code> directory; the <code>Makefile</code> does not create it.</li> <li><code>test/test_matmul.py</code> contains a function named <code>test_matadd</code>, so the filename and exported test name do not match.</li> <li>The source is SystemVerilog, while parts of the README describe the project more loosely as \u201cVerilog.\u201d The actual flow uses <code>sv2v</code> first, then <code>iverilog</code>.</li> </ul>"},{"location":"report/00-executive-overview/#overall-assessment","title":"Overall assessment","text":"<p>This is a disciplined educational hardware repository with a clear module boundary at the GPU/core/thread levels, a compact verification surface, and unusually readable conceptual documentation. Its main rough edge is not architectural chaos; it is the gap between conceptual discussion in the README and what is concretely implemented in the checked-in RTL.</p>"},{"location":"report/01-repository-structure/","title":"Repository Structure","text":""},{"location":"report/01-repository-structure/#top-level-layout","title":"Top-level layout","text":"<p>Confirmed from the local checkout:</p> <pre><code>.\n\u251c\u2500\u2500 docs/\n\u2502   \u251c\u2500\u2500 images/\n\u2502   \u2514\u2500\u2500 report/\n\u251c\u2500\u2500 gds/\n\u2502   \u251c\u2500\u2500 0/gpu.gds\n\u2502   \u2514\u2500\u2500 1/gpu.gds\n\u251c\u2500\u2500 Makefile\n\u251c\u2500\u2500 README.md\n\u251c\u2500\u2500 src/\n\u2514\u2500\u2500 test/\n</code></pre>"},{"location":"report/01-repository-structure/#directory-roles","title":"Directory roles","text":""},{"location":"report/01-repository-structure/#src","title":"<code>src/</code>","text":"<p>This is the complete checked-in RTL implementation:</p> <ul> <li><code>gpu.sv</code> \u2014 top-level integration and external memory/control interface</li> <li><code>core.sv</code> \u2014 per-core composition boundary</li> <li><code>dispatch.sv</code> \u2014 block dispatch and completion tracking</li> <li><code>controller.sv</code> \u2014 shared memory arbitration primitive for program/data memory</li> <li><code>dcr.sv</code> \u2014 device control register storing <code>thread_count</code></li> <li><code>scheduler.sv</code> \u2014 core execution state machine</li> <li><code>fetcher.sv</code> \u2014 instruction fetch unit</li> <li><code>decoder.sv</code> \u2014 ISA decode logic</li> <li><code>registers.sv</code> \u2014 per-thread register file plus SIMD metadata registers</li> <li><code>pc.sv</code> \u2014 next-PC and NZP handling</li> <li><code>lsu.sv</code> \u2014 per-thread load/store unit</li> <li><code>alu.sv</code> \u2014 per-thread arithmetic unit</li> </ul> <p>There is no <code>cache.sv</code> or similar cache implementation in the current tree.</p>"},{"location":"report/01-repository-structure/#test","title":"<code>test/</code>","text":"<p>This directory holds the cocotb testbench and helper code:</p> <ul> <li><code>test_matadd.py</code> \u2014 matrix-add kernel simulation</li> <li><code>test_matmul.py</code> \u2014 matrix-multiply kernel simulation</li> <li><code>helpers/setup.py</code> \u2014 clock/reset/program/data/thread-count setup</li> <li><code>helpers/memory.py</code> \u2014 software-backed program/data memory model</li> <li><code>helpers/format.py</code> \u2014 trace-format helpers</li> <li><code>helpers/logger.py</code> \u2014 log writer for execution traces</li> <li><code>logs/.gitkeep</code> \u2014 retained log output directory</li> </ul> <p>The tests act as the \u201chost system\u201d for the GPU by loading memory contents, writing the thread count through the device-control interface, asserting <code>start</code>, and then emulating memory readiness and responses in Python.</p>"},{"location":"report/01-repository-structure/#docs","title":"<code>docs/</code>","text":"<ul> <li><code>docs/images/</code> contains the static diagrams referenced by the project README.</li> <li><code>docs/report/</code> contains this source-verified narrative analysis set.</li> </ul>"},{"location":"report/01-repository-structure/#gds","title":"<code>gds/</code>","text":"<p><code>gds/0/gpu.gds</code> and <code>gds/1/gpu.gds</code> are physical-layout artifacts. The repository does not explain how they were generated or what distinguishes the two directories, so they should be treated as concrete artifacts with undocumented provenance.</p>"},{"location":"report/01-repository-structure/#supporting-project-files","title":"Supporting project files","text":""},{"location":"report/01-repository-structure/#readmemd","title":"<code>README.md</code>","text":"<p>Acts as the main conceptual document. It explains the motivation for the project, the top-level architecture, the ISA, the example kernels, the simulation flow, and a roadmap of future enhancements.</p>"},{"location":"report/01-repository-structure/#makefile","title":"<code>Makefile</code>","text":"<p>Encodes the practical compile and test entrypoints. It transpiles SystemVerilog with <code>sv2v</code>, compiles the generated Verilog with <code>iverilog</code>, and launches cocotb via <code>vvp</code>.</p>"},{"location":"report/01-repository-structure/#gitignore","title":"<code>.gitignore</code>","text":"<p>Ignores Python caches, <code>build/</code>, generated logs, some generated GDS-adjacent artifacts, <code>.DS_Store</code>, and <code>results.xml</code>.</p>"},{"location":"report/01-repository-structure/#module-hierarchy-snapshot","title":"Module hierarchy snapshot","text":"<pre><code>flowchart TD\n    GPU[gpu.sv] --&gt; DCR[dcr.sv]\n    GPU --&gt; Dispatch[dispatch.sv]\n    GPU --&gt; CtrlData[controller.sv\\n(data memory)]\n    GPU --&gt; CtrlProg[controller.sv\\n(program memory)]\n    GPU --&gt; Core[core.sv x NUM_CORES]\n\n    Core --&gt; Scheduler[scheduler.sv]\n    Core --&gt; Fetcher[fetcher.sv]\n    Core --&gt; Decoder[decoder.sv]\n    Core --&gt; ALU[alu.sv x THREADS_PER_BLOCK]\n    Core --&gt; LSU[lsu.sv x THREADS_PER_BLOCK]\n    Core --&gt; Regs[registers.sv x THREADS_PER_BLOCK]\n    Core --&gt; PC[pc.sv x THREADS_PER_BLOCK]</code></pre>"},{"location":"report/01-repository-structure/#architecture-seams-that-map-cleanly-to-documentation","title":"Architecture seams that map cleanly to documentation","text":"<ul> <li>Host/control seam \u2014 <code>start</code>, <code>done</code>, and the device control register</li> <li>Dispatch seam \u2014 block formation and assignment to cores</li> <li>Core execution seam \u2014 fetch/decode/schedule/update lifecycle</li> <li>Memory seam \u2014 internal requesters versus external memory interfaces</li> <li>Thread-local seam \u2014 replicated ALU/LSU/register/PC resources under shared scheduling</li> <li>Verification seam \u2014 RTL DUT versus Python memory model and trace logging</li> </ul>"},{"location":"report/02-architecture-and-execution/","title":"Architecture and Execution","text":""},{"location":"report/02-architecture-and-execution/#top-level-architecture","title":"Top-level architecture","text":"<p>The design centers on the <code>gpu</code> module in <code>src/gpu.sv</code>. External DeepWiki material describes the project using the same broad decomposition seen in the local RTL: top-level GPU control, block dispatch, memory arbitration, and per-core execution resources. The wording in this report is still grounded primarily in the checked-in source.</p> <p>The top-level interface makes the execution model explicit:</p> <ul> <li>host-side control (<code>start</code>, <code>done</code>, and the device-control write interface)</li> <li>external program-memory read interface</li> <li>external data-memory read/write interface</li> </ul> <p>The top-level module wires together four major subsystems:</p> <ol> <li><code>dcr</code> \u2014 stores the thread count for the active kernel</li> <li><code>dispatch</code> \u2014 divides threads into blocks and assigns them to cores</li> <li><code>controller</code> instances \u2014 arbitrate between internal requesters and external memory channels</li> <li><code>core</code> instances \u2014 execute blocks of threads</li> </ol>"},{"location":"report/02-architecture-and-execution/#system-architecture-diagram","title":"System architecture diagram","text":"<pre><code>flowchart TD\n    Host[Host / Testbench] --&gt; DCR[Device Control Register]\n    Host --&gt; PM[Program Memory]\n    Host --&gt; DM[Data Memory]\n    Host --&gt; Start[start]\n\n    DCR --&gt; Dispatch[Dispatcher]\n    Start --&gt; Dispatch\n    Dispatch --&gt; Cores[Compute Cores]\n\n    Cores --&gt; ProgCtrl[Program Memory Controller]\n    ProgCtrl --&gt; PM\n\n    Cores --&gt; DataCtrl[Data Memory Controller]\n    DataCtrl --&gt; DM\n\n    subgraph CoreInternals[One Core]\n        Scheduler[Scheduler] --&gt; Fetcher[Fetcher]\n        Scheduler --&gt; Decoder[Decoder]\n        Scheduler --&gt; Threads[Per-thread ALU / LSU / Registers / PC]\n    end</code></pre>"},{"location":"report/02-architecture-and-execution/#confirmed-execution-model","title":"Confirmed execution model","text":"<p>The core execution pipeline is not just described in prose; it is encoded directly in <code>src/scheduler.sv</code> as a state machine:</p> <ul> <li><code>IDLE</code></li> <li><code>FETCH</code></li> <li><code>DECODE</code></li> <li><code>REQUEST</code></li> <li><code>WAIT</code></li> <li><code>EXECUTE</code></li> <li><code>UPDATE</code></li> <li><code>DONE</code></li> </ul> <p>The scheduler processes one block at a time through these stages. <code>WAIT</code> explicitly inspects LSU state and does not advance until no thread is still waiting on memory.</p>"},{"location":"report/02-architecture-and-execution/#instruction-flow","title":"Instruction flow","text":"<pre><code>flowchart LR\n    Idle[IDLE] --&gt; Fetch[FETCH]\n    Fetch --&gt; Decode[DECODE]\n    Decode --&gt; Request[REQUEST]\n    Request --&gt; Wait[WAIT]\n    Wait --&gt; Execute[EXECUTE]\n    Execute --&gt; Update[UPDATE]\n    Update --&gt;|RET or block complete| Done[DONE]\n    Update --&gt;|next instruction| Fetch</code></pre>"},{"location":"report/02-architecture-and-execution/#per-core-organization","title":"Per-core organization","text":"<p>Each <code>core</code> contains:</p> <ul> <li>one <code>fetcher</code></li> <li>one <code>decoder</code></li> <li>one <code>scheduler</code></li> <li>one ALU per supported thread</li> <li>one LSU per supported thread</li> <li>one register file per supported thread</li> <li>one PC unit per supported thread</li> </ul> <p>This replication is created with a <code>generate</code> block in <code>src/core.sv</code>, so the amount of thread-local hardware scales with <code>THREADS_PER_BLOCK</code>.</p>"},{"location":"report/02-architecture-and-execution/#core-to-thread-decomposition","title":"Core-to-thread decomposition","text":"<pre><code>flowchart TD\n    Core[core.sv] --&gt; Fetcher[fetcher.sv]\n    Core --&gt; Decoder[decoder.sv]\n    Core --&gt; Scheduler[scheduler.sv]\n    Core --&gt; ThreadUnits[Thread units]\n\n    ThreadUnits --&gt; ALU[alu.sv]\n    ThreadUnits --&gt; LSU[lsu.sv]\n    ThreadUnits --&gt; Registers[registers.sv]\n    ThreadUnits --&gt; PC[pc.sv]</code></pre>"},{"location":"report/02-architecture-and-execution/#simd-style-thread-model","title":"SIMD-style thread model","text":"<p>The SIMD flavor comes from replicated per-thread state plus shared control flow. Confirmed details from <code>src/registers.sv</code>:</p> <ul> <li>16 registers per thread</li> <li><code>R0</code> through <code>R12</code> are writable general-purpose registers</li> <li>register 13 stores <code>%blockIdx</code></li> <li>register 14 stores <code>%blockDim</code></li> <li>register 15 stores <code>%threadIdx</code></li> </ul> <p>This lets each thread execute the same decoded instruction stream while operating on different local data.</p>"},{"location":"report/02-architecture-and-execution/#dispatch-behavior","title":"Dispatch behavior","text":"<p><code>src/dispatch.sv</code> computes the number of blocks as:</p> <pre><code>total_blocks = (thread_count + THREADS_PER_BLOCK - 1) / THREADS_PER_BLOCK\n</code></pre> <p>It then assigns blocks to available cores, tracking both <code>blocks_dispatched</code> and <code>blocks_done</code>. The final block may contain fewer threads than <code>THREADS_PER_BLOCK</code>; that case is handled by passing a reduced <code>core_thread_count</code>.</p>"},{"location":"report/02-architecture-and-execution/#memory-system","title":"Memory system","text":"<p>The memory model is intentionally simple but concrete.</p>"},{"location":"report/02-architecture-and-execution/#program-memory","title":"Program memory","text":"<ul> <li>read-only at the controller level (<code>WRITE_ENABLE = 0</code> in <code>gpu.sv</code>)</li> <li>one fetcher per core issues requests</li> <li>fetched instructions are 16 bits wide</li> </ul>"},{"location":"report/02-architecture-and-execution/#data-memory","title":"Data memory","text":"<ul> <li>read/write through LSU-generated requests</li> <li>multiple LSU consumers share a controller</li> <li>the controller tracks which consumer each channel is currently serving</li> </ul> <p>The <code>controller</code> module is therefore a reusable arbitration layer rather than a one-off memory wrapper.</p>"},{"location":"report/02-architecture-and-execution/#memory-and-control-relationships","title":"Memory and control relationships","text":"<pre><code>flowchart TD\n    Fetcher[Fetcher] --&gt; ProgCtrl[Program Memory Controller]\n    ProgCtrl --&gt; ProgMem[Program Memory]\n\n    LSUs[Per-thread LSUs] --&gt; DataCtrl[Data Memory Controller]\n    DataCtrl --&gt; DataMem[Data Memory]\n\n    Scheduler[Scheduler] --&gt; Fetcher\n    Scheduler --&gt; LSUs\n    Decoder[Decoder] --&gt; LSUs</code></pre>"},{"location":"report/02-architecture-and-execution/#branching-and-convergence","title":"Branching and convergence","text":"<p>The design supports <code>CMP</code> and <code>BRnzp</code>, but branch handling is intentionally naive.</p> <p>Confirmed from <code>src/scheduler.sv</code>, <code>src/pc.sv</code>, and <code>src/core.sv</code>:</p> <ul> <li>each thread computes its own <code>next_pc</code></li> <li>the scheduler updates the shared <code>current_pc</code> specifically from <code>next_pc[THREADS_PER_BLOCK-1]</code></li> <li>the source contains an explicit TODO around branch divergence</li> </ul> <p>The practical interpretation is that the current implementation assumes per-block control-flow convergence rather than supporting true branch divergence.</p> <p>There is also a narrower implementation caveat for short final blocks: <code>pc</code> instances are enabled only when <code>i &lt; thread_count</code> in <code>src/core.sv</code>, but the scheduler still takes <code>next_pc[THREADS_PER_BLOCK-1]</code> as the block-wide next PC. In a partial block, that means the shared PC is sourced from the last thread slot even when that slot is not active.</p>"},{"location":"report/02-architecture-and-execution/#isa-implementation-notes","title":"ISA implementation notes","text":"<p><code>src/decoder.sv</code> decodes the following opcode set:</p> <ul> <li><code>NOP</code></li> <li><code>BRnzp</code></li> <li><code>CMP</code></li> <li><code>ADD</code></li> <li><code>SUB</code></li> <li><code>MUL</code></li> <li><code>DIV</code></li> <li><code>LDR</code></li> <li><code>STR</code></li> <li><code>CONST</code></li> <li><code>RET</code></li> </ul> <p>That matches the ISA described in the README.</p>"},{"location":"report/02-architecture-and-execution/#deepwiki-aligned-interpretation","title":"DeepWiki-aligned interpretation","text":"<p>DeepWiki\u2019s external documentation is useful here because it reinforces the same top-level understanding without changing the source-grounded conclusions:</p> <ul> <li><code>gpu.sv</code> is the orchestration boundary</li> <li><code>dispatch.sv</code> is the scheduling boundary between kernel-wide work and core-local work</li> <li><code>core.sv</code> is the composition boundary for execution resources</li> <li><code>scheduler.sv</code> is the canonical control-flow definition for instruction progression</li> <li><code>controller.sv</code> is the memory arbitration boundary shared by instruction and data access paths</li> </ul>"},{"location":"report/02-architecture-and-execution/#implemented-behavior-versus-conceptual-roadmap","title":"Implemented behavior versus conceptual roadmap","text":"<p>The README discusses cache, branch divergence, memory coalescing, pipelining, and warp scheduling. In the checked-in RTL, those are future-facing concepts rather than implemented subsystems. Any architecture explanation should therefore separate:</p> <ul> <li>implemented today \u2014 the modules in <code>src/</code> and their current behaviors</li> <li>described conceptually \u2014 the README\u2019s explanations of how real GPUs are optimized</li> <li>planned work \u2014 items called out in the README\u2019s future-facing sections</li> </ul>"},{"location":"report/03-build-and-test-workflow/","title":"Build and Test Workflow","text":""},{"location":"report/03-build-and-test-workflow/#toolchain","title":"Toolchain","text":"<p>The current repository workflow depends on:</p> <ul> <li><code>sv2v</code></li> <li><code>iverilog</code></li> <li><code>vvp</code></li> <li><code>cocotb</code></li> <li>Python for the cocotb tests and helpers</li> <li>optionally <code>gtkwave</code> for waveform inspection</li> </ul> <p>The README also instructs users to create a <code>build/</code> directory manually before running the flow.</p>"},{"location":"report/03-build-and-test-workflow/#confirmed-makefile-behavior","title":"Confirmed Makefile behavior","text":"<p>From <code>Makefile</code>:</p>"},{"location":"report/03-build-and-test-workflow/#make-compile","title":"<code>make compile</code>","text":"<ol> <li>Runs <code>make compile_alu</code></li> <li>Runs <code>sv2v -I src/* -w build/gpu.v</code></li> <li>Appends <code>build/alu.v</code> into <code>build/gpu.v</code></li> <li>Prepends a <code>`timescale 1ns/1ns</code> line via a temporary file</li> </ol>"},{"location":"report/03-build-and-test-workflow/#make-test_matadd-make-test_matmul","title":"<code>make test_matadd</code> / <code>make test_matmul</code>","text":"<ol> <li>Calls <code>make compile</code></li> <li>Runs:</li> </ol> <pre><code>iverilog -o build/sim.vvp -s gpu -g2012 build/gpu.v\n</code></pre> <ol> <li>Runs cocotb through <code>vvp</code> with:</li> </ol> <pre><code>MODULE=test.test_$* vvp -M $(cocotb-config --prefix)/cocotb/libs -m libcocotbvpi_icarus build/sim.vvp\n</code></pre> <p>That means <code>make test_matadd</code> loads <code>test.test_matadd</code>, and <code>make test_matmul</code> loads <code>test.test_matmul</code>.</p>"},{"location":"report/03-build-and-test-workflow/#tool-flow-diagram","title":"Tool flow diagram","text":"<pre><code>flowchart TD\n    Start[make test_matadd or make test_matmul] --&gt; Compile[make compile]\n    Compile --&gt; SV2V[sv2v converts SystemVerilog to Verilog]\n    SV2V --&gt; Icarus[iverilog builds build/sim.vvp]\n    Icarus --&gt; VVP[vvp launches simulator runtime]\n    VVP --&gt; Cocotb[cocotb test module runs]\n    Cocotb --&gt; Memory[Python memory models serve program/data requests]\n    Cocotb --&gt; Logs[test/logs/log_*.txt]</code></pre>"},{"location":"report/03-build-and-test-workflow/#testbench-behavior","title":"Testbench behavior","text":"<p>The cocotb setup in <code>test/helpers/setup.py</code> does the following:</p> <ol> <li>starts a clock with <code>Clock(dut.clk, 25, units=\"us\")</code></li> <li>asserts then deasserts reset</li> <li>loads program memory contents from a Python list</li> <li>loads data memory contents from a Python list</li> <li>writes the desired thread count into the DUT device-control register</li> <li>asserts <code>dut.start</code></li> </ol> <p><code>setup()</code> does not deassert <code>dut.start</code> afterward, so the testbench effectively drives a level-high start condition for the remainder of the run.</p> <p>The main test loop then repeatedly:</p> <ul> <li>runs the software memory models (<code>program_memory.run()</code> and <code>data_memory.run()</code>)</li> <li>waits for a read-only phase</li> <li>logs the trace state</li> <li>advances one clock edge</li> </ul> <p>Execution ends when <code>dut.done</code> becomes <code>1</code>.</p>"},{"location":"report/03-build-and-test-workflow/#why-the-cocotb-structure-matches-the-official-model","title":"Why the cocotb structure matches the official model","text":"<p>The repository\u2019s cocotb code follows the documented cocotb pattern closely:</p> <ul> <li>tests are <code>async</code> Python coroutines</li> <li>DUT signals are accessed as <code>dut.&lt;signal&gt;.value</code></li> <li>the testbench uses <code>Clock</code> to drive <code>dut.clk</code></li> <li>synchronization uses triggers such as <code>RisingEdge</code> and read-only phases</li> </ul> <p>That is exactly the kind of flow cocotb\u2019s documentation presents for coroutine-based HDL tests.</p>"},{"location":"report/03-build-and-test-workflow/#why-the-icarus-command-line-matches-the-official-model","title":"Why the Icarus command line matches the official model","text":"<p>The <code>iverilog</code> invocation also matches the documented usage:</p> <ul> <li><code>-g2012</code> enables IEEE 1800-2012 / SystemVerilog language support</li> <li><code>-s gpu</code> explicitly selects <code>gpu</code> as the root module for elaboration</li> <li><code>vvp -M ... -m libcocotbvpi_icarus</code> loads the cocotb VPI module into the simulator runtime</li> </ul> <p>This is the key bridge between the generated Verilog simulation and the Python cocotb test process.</p>"},{"location":"report/03-build-and-test-workflow/#sv2v-in-this-repository","title":"<code>sv2v</code> in this repository","text":"<p>The <code>sv2v</code> project documents itself as a SystemVerilog-to-Verilog converter aimed at synthesizable constructs, with <code>-w</code> used to write output files and <code>-I</code> / <code>--incdir</code> used to extend the include search path. That explains the intent of this repository\u2019s compile stage, which as written emits generated Verilog into <code>build/</code> before invoking <code>iverilog</code>.</p> <p>One subtle but important nuance from the official <code>sv2v</code> usage docs: users usually pass all relevant SystemVerilog files together so the converter can resolve cross-file constructs. This repository instead runs the atypical compile rules exactly as written in the <code>Makefile</code>: <code>alu.sv</code> is converted separately and appended into <code>build/gpu.v</code>, while the main <code>sv2v -I src/* -w build/gpu.v</code> invocation does not explicitly list input files in the rule itself. The report therefore describes the Makefile faithfully, but should not imply that this is the standard or obviously complete <code>sv2v</code> invocation pattern.</p>"},{"location":"report/03-build-and-test-workflow/#memory-simulation-strategy","title":"Memory simulation strategy","text":"<p>The hardware does not connect to an external simulator-owned memory model directly. Instead, <code>test/helpers/memory.py</code> emulates the external memories in Python by:</p> <ul> <li>decoding DUT read-valid signals and addresses</li> <li>returning ready/data when a valid request is present</li> <li>updating internal Python memory arrays on writes</li> </ul> <p>This keeps the tests compact and makes memory behavior easy to inspect in logs, but it also means timing is idealized compared with a more realistic external-memory model.</p>"},{"location":"report/03-build-and-test-workflow/#output-artifacts","title":"Output artifacts","text":"<p>The logger in <code>test/helpers/logger.py</code> writes trace logs to:</p> <pre><code>test/logs/log_&lt;timestamp&gt;.txt\n</code></pre> <p>These logs include:</p> <ul> <li>memory dumps</li> <li>instruction traces</li> <li>core state</li> <li>fetcher and LSU state</li> <li>register contents</li> </ul> <p>If waveform dumping is enabled separately, GTKWave is the natural viewer for offline inspection of those dump files. GTKWave\u2019s own docs describe it as a post-mortem waveform browser for formats such as VCD and related traces.</p>"},{"location":"report/03-build-and-test-workflow/#external-reference-map","title":"External reference map","text":"Tool Repo use Most relevant official concepts <code>cocotb</code> Python coroutine testbench <code>@cocotb.test</code>, <code>Clock</code>, <code>RisingEdge</code>, <code>ReadOnly</code>, DUT signal access <code>iverilog</code> compile generated Verilog <code>-g2012</code>, <code>-s &lt;topmodule&gt;</code>, Verilog/SystemVerilog elaboration <code>vvp</code> run compiled simulation <code>-M</code> module search path, <code>-m</code> load VPI module <code>sv2v</code> convert SystemVerilog before compile <code>-w</code> output file, <code>-I/--incdir</code>, synthesizable SystemVerilog focus <code>gtkwave</code> optional debug viewer offline waveform browsing and signal inspection"},{"location":"report/03-build-and-test-workflow/#notable-rough-edges","title":"Notable rough edges","text":"<ul> <li>The <code>Makefile</code> does not create <code>build/</code>, so a first-time user who skips the README setup step will fail early.</li> <li><code>test/test_matmul.py</code> exports a test function named <code>test_matadd</code>, which is confusing even though the module name still determines which file cocotb loads.</li> <li>The flow is optimized for local learning and simulation, not for a polished CI or packaging experience.</li> </ul>"},{"location":"report/04-risks-open-questions/","title":"Risks, Limitations, and Open Questions","text":""},{"location":"report/04-risks-open-questions/#confirmed-current-limitations","title":"Confirmed current limitations","text":"<p>These points are supported by the checked-in source, not merely by the README\u2019s narrative.</p>"},{"location":"report/04-risks-open-questions/#1-no-implemented-cache-module","title":"1. No implemented cache module","text":"<p>The README describes cache as part of the architecture, but the source tree contains no cache module and <code>gpu.sv</code> instantiates no cache layer. This is the single largest source-to-documentation mismatch in the repo.</p>"},{"location":"report/04-risks-open-questions/#2-no-branch-divergence-support","title":"2. No branch divergence support","text":"<p><code>scheduler.sv</code> contains an explicit TODO indicating that branch divergence is not implemented and that the current design assumes next-PC convergence. That keeps the design simple, but it also means kernels that require divergent per-thread control flow are out of scope.</p>"},{"location":"report/04-risks-open-questions/#3-no-pipelining-or-overlap-beyond-basic-async-memory-waiting","title":"3. No pipelining or overlap beyond basic async memory waiting","text":"<p>The scheduler waits for one instruction lifecycle to finish before progressing to the next instruction. That matches the repository\u2019s educational goal, but it also means throughput-oriented optimizations are intentionally absent.</p>"},{"location":"report/04-risks-open-questions/#4-simple-memory-timing-assumptions-in-tests","title":"4. Simple memory timing assumptions in tests","text":"<p>The Python memory model responds as soon as valid requests are observed. This is good for clarity, but it idealizes memory timing and does not stress the design with realistic latency or backpressure behavior.</p>"},{"location":"report/04-risks-open-questions/#documented-versus-implemented","title":"Documented versus implemented","text":"<pre><code>flowchart LR\n    Docs[README conceptual model] --&gt; Shared[Current overlap]\n    Source[src/ implemented RTL] --&gt; Shared\n\n    Docs --&gt; Roadmap[Cache / divergence / coalescing / pipelining / warp scheduling]\n    Source --&gt; Current[DCR / dispatch / controllers / cores / scheduler / cocotb harness]</code></pre> <p>The safest interpretation is:</p> <ul> <li>treat <code>README.md</code> as the conceptual guide</li> <li>treat <code>src/</code> as the source of truth for implemented behavior</li> <li>treat advanced features as roadmap items unless concrete RTL supports them</li> </ul>"},{"location":"report/04-risks-open-questions/#repository-inconsistencies-worth-preserving-in-docs","title":"Repository inconsistencies worth preserving in docs","text":""},{"location":"report/04-risks-open-questions/#readme-uses-present-tense-architecture-language-for-cache","title":"README uses present-tense architecture language for cache","text":"<p>That wording can lead a new reader to expect cache logic in <code>src/</code>, but the current checkout does not support that expectation.</p>"},{"location":"report/04-risks-open-questions/#testtest_matmulpy-naming-mismatch","title":"<code>test/test_matmul.py</code> naming mismatch","text":"<p>The file is clearly the matrix-multiplication test, but its exported cocotb function is named <code>test_matadd</code>. That does not invalidate the test flow, yet it is still a maintenance smell.</p>"},{"location":"report/04-risks-open-questions/#build-directory-prerequisite-lives-in-readme-not-in-automation","title":"Build-directory prerequisite lives in README, not in automation","text":"<p>The current flow assumes <code>build/</code> already exists. A more ergonomic local workflow would create it from the Makefile itself.</p>"},{"location":"report/04-risks-open-questions/#open-questions-raised-by-the-current-checkout","title":"Open questions raised by the current checkout","text":""},{"location":"report/04-risks-open-questions/#gds-provenance","title":"GDS provenance","text":"<p><code>gds/0/gpu.gds</code> and <code>gds/1/gpu.gds</code> exist, but the repository does not explain:</p> <ul> <li>what flow produced them</li> <li>what distinguishes <code>0/</code> from <code>1/</code></li> <li>whether they represent revisions, experiments, or packaging targets</li> </ul>"},{"location":"report/04-risks-open-questions/#physical-design-context","title":"Physical-design context","text":"<p>There are hints that physical-design constraints shaped parts of the RTL, but the repository does not include a dedicated note explaining the larger downstream flow.</p>"},{"location":"report/04-risks-open-questions/#scope-of-future-work-versus-architectural-commitments","title":"Scope of future work versus architectural commitments","text":"<p>The README\u2019s \u201cNext Steps\u201d section mentions cache, branch divergence, memory coalescing, pipelining, graphics, and other extensions. Those should be read as roadmap intent, not as partially implemented subsystems, unless the source clearly shows otherwise.</p>"},{"location":"report/04-risks-open-questions/#recommended-interpretation-for-future-readers","title":"Recommended interpretation for future readers","text":"<p>This repository is best understood as a compact educational GPU whose main value is architectural clarity. The code is concrete enough to simulate real kernels, but many of the README\u2019s advanced topics are there to teach what real GPUs do next, not to claim that tiny-gpu already does it.</p>"},{"location":"report/05-matadd-trace-walkthrough/","title":"matadd Execution Trace Walkthrough","text":""},{"location":"report/05-matadd-trace-walkthrough/#goal-of-this-report","title":"Goal of this report","text":"<p>This chapter explains how to read the <code>matadd</code> simulation trace as a beginner.</p> <p>Instead of translating every repeated log line, it focuses on the moments that actually teach you how the design works:</p> <ul> <li>how the testbench sets up the run</li> <li>what one scheduler cycle means</li> <li>why the first real instruction does not appear immediately</li> <li>how eight threads move through the same instruction stream</li> <li>when the actual data-memory writes happen</li> </ul> <p>All observations below are grounded in the log file from a fresh run:</p> <ul> <li>log: <code>test/logs/log_20260402201022.txt</code></li> <li>completed in: <code>178 cycles</code></li> </ul>"},{"location":"report/05-matadd-trace-walkthrough/#what-this-test-is-trying-to-prove","title":"What this test is trying to prove","text":"<p><code>test/test_matadd.py</code> loads a tiny kernel which adds two 1\u00d78 vectors stored in data memory.</p> <p>The input layout is:</p> <ul> <li>addresses <code>0..7</code>: matrix A = <code>0 1 2 3 4 5 6 7</code></li> <li>addresses <code>8..15</code>: matrix B = <code>0 1 2 3 4 5 6 7</code></li> <li>addresses <code>16..23</code>: output buffer, initially all zero</li> </ul> <p>The expected result is:</p> <pre><code>0 2 4 6 8 10 12 14\n</code></pre> <p>The final memory dump in the log confirms exactly that:</p> <ul> <li><code>data[16..23] = 0, 2, 4, 6, 8, 10, 12, 14</code></li> </ul>"},{"location":"report/05-matadd-trace-walkthrough/#what-the-testbench-does-every-cycle","title":"What the testbench does every cycle","text":"<p>The cocotb harness is simple but very important to understand.</p> <p>From <code>test/helpers/setup.py</code> and <code>test/test_matadd.py</code>, the flow is:</p> <ol> <li>start the clock</li> <li>pulse reset</li> <li>load program memory with the kernel instructions</li> <li>load data memory with A and B</li> <li>write <code>threads = 8</code> into the device control register</li> <li>raise <code>start</code></li> <li>on every loop iteration:</li> <li><code>data_memory.run(cycle=cycles)</code> services data-memory reads/writes</li> <li><code>program_memory.run()</code> services instruction fetches</li> <li><code>format_cycle(dut, cycles)</code> logs the current machine state</li> <li>then the test advances one rising edge</li> </ol> <p>That means the trace is a combined view of:</p> <ul> <li>the RTL state inside the GPU</li> <li>the Python-side memory model acting like \u201cexternal memory\u201d</li> </ul>"},{"location":"report/05-matadd-trace-walkthrough/#how-to-read-one-trace-block","title":"How to read one trace block","text":"<p>Every cycle dump follows the same structure.</p> <p>For each active core, and then for each active thread in that core, the logger shows:</p> <ul> <li><code>PC</code></li> <li>decoded <code>Instruction</code></li> <li><code>Core State</code></li> <li><code>Fetcher State</code></li> <li><code>LSU State</code></li> <li>all register values</li> <li>selected datapath outputs like <code>ALU Out</code>, <code>LSU Out</code>, or <code>Constant</code></li> </ul> <p>For this design, the scheduler state machine is the key rhythm:</p> <pre><code>FETCH -&gt; DECODE -&gt; REQUEST -&gt; WAIT -&gt; EXECUTE -&gt; UPDATE\n</code></pre> <p>Even pure ALU or CONST instructions still pass through this full sequence, because the scheduler is intentionally simple and uniform.</p>"},{"location":"report/05-matadd-trace-walkthrough/#step-1-initial-memory-state-and-idle-machine","title":"Step 1: initial memory state and idle machine","text":"<p>At the very top of the log, before cycle 0, the testbench prints the initial data memory table.</p> <p>Then cycle 0 shows both cores still idle.</p> <p>Important details from cycle 0:</p> <ul> <li>Core 0 contains logical threads <code>0..3</code></li> <li>Core 1 contains logical threads <code>4..7</code></li> <li><code>%blockDim = 4</code>, so each core handles a block of four threads</li> <li><code>%blockIdx = 0</code> on Core 0 and <code>%blockIdx = 1</code> on Core 1 once dispatch is active</li> </ul> <p>At cycle 0 the trace still shows <code>Instruction: NOP</code> and <code>Core State: IDLE</code>. That is not a bug. It just means the kernel launch has been requested, but the fetch pipeline has not yet produced the first instruction word.</p>"},{"location":"report/05-matadd-trace-walkthrough/#step-2-why-the-first-real-instruction-appears-at-cycle-7","title":"Step 2: why the first real instruction appears at cycle 7","text":"<p>From cycles <code>0..6</code>, the machine transitions from <code>IDLE</code> into fetch activity.</p> <p>At cycle 6, both cores are already in:</p> <ul> <li><code>Core State: FETCH</code></li> <li><code>Fetcher State: FETCHING</code></li> </ul> <p>But the visible instruction is still <code>NOP</code>.</p> <p>At cycle 7, the first real instruction appears:</p> <pre><code>PC: 0\nInstruction: MUL R0, %blockIdx, %blockDim\nCore State: FETCH\nFetcher State: FETCHED\n</code></pre> <p>This is the first good example of how to read the fetcher:</p> <ul> <li><code>FETCHING</code> means the request is still in flight</li> <li><code>FETCHED</code> means the instruction bits have arrived and can now be decoded on later cycles</li> </ul> <p>So the reason the log starts with several cycles of <code>NOP</code> is simply that the fetch pipeline takes a few cycles before the first instruction becomes visible in the core.</p>"},{"location":"report/05-matadd-trace-walkthrough/#step-3-one-instruction-takes-six-scheduler-phases","title":"Step 3: one instruction takes six scheduler phases","text":"<p>The first instruction is:</p> <pre><code>MUL R0, %blockIdx, %blockDim\n</code></pre> <p>Conceptually, this computes the block base index:</p> <pre><code>R0 = blockIdx * blockDim\n</code></pre> <p>In the trace, one instruction does not complete in one cycle. It walks through the scheduler phases:</p> <ul> <li><code>FETCH</code></li> <li><code>DECODE</code></li> <li><code>REQUEST</code></li> <li><code>WAIT</code></li> <li><code>EXECUTE</code></li> <li><code>UPDATE</code></li> </ul> <p>This is why a single kernel instruction occupies many trace entries. The trace is showing the scheduler micro-steps, not just ISA-level steps.</p>"},{"location":"report/05-matadd-trace-walkthrough/#step-4-how-the-eight-threads-get-different-indices-while-sharing-the-same-instruction-stream","title":"Step 4: how the eight threads get different indices while sharing the same instruction stream","text":"<p>The second kernel instruction is:</p> <pre><code>ADD R0, R0, %threadIdx\n</code></pre> <p>That is the classic GPU indexing step:</p> <pre><code>i = blockIdx * blockDim + threadIdx\n</code></pre> <p>Because all threads execute the same instruction but each thread has a different <code>%threadIdx</code>, the register values diverge naturally.</p> <p>For example:</p> <ul> <li>thread 0 ends with <code>R0 = 0</code></li> <li>thread 1 ends with <code>R0 = 1</code></li> <li>...</li> <li>thread 7 ends with <code>R0 = 7</code></li> </ul> <p>This is one of the most important \u201cGPU ideas\u201d visible in the trace:</p> <p>same instruction stream, different per-thread register state</p>"},{"location":"report/05-matadd-trace-walkthrough/#step-5-loading-constants-establishes-the-memory-layout","title":"Step 5: loading constants establishes the memory layout","text":"<p>The three <code>CONST</code> instructions load:</p> <ul> <li><code>R1 = 0</code>  \u2192 base of A</li> <li><code>R2 = 8</code>  \u2192 base of B</li> <li><code>R3 = 16</code> \u2192 base of C</li> </ul> <p>After that, each thread can compute addresses using only register arithmetic.</p> <p>This is why later <code>ADD</code> instructions are easy to interpret:</p> <ul> <li><code>ADD R4, R1, R0</code> means \u201caddress of A[i]\u201d</li> <li><code>ADD R5, R2, R0</code> means \u201caddress of B[i]\u201d</li> <li><code>ADD R7, R3, R0</code> means \u201caddress of C[i]\u201d</li> </ul>"},{"location":"report/05-matadd-trace-walkthrough/#step-6-the-first-real-memory-dependent-phase-is-ldr-r4-r4","title":"Step 6: the first real memory-dependent phase is <code>LDR R4, R4</code>","text":"<p>A very educational trace point appears around cycle 73.</p> <p>At that point, Core 0 shows:</p> <pre><code>PC: 6\nInstruction: LDR R4, R4\n</code></pre> <p>And each thread already has a different <code>R4</code>:</p> <ul> <li>thread 0: <code>R4 = 0</code></li> <li>thread 1: <code>R4 = 1</code></li> <li>thread 2: <code>R4 = 2</code></li> <li>thread 3: <code>R4 = 3</code></li> </ul> <p>So this one ISA instruction means:</p> <ul> <li>thread 0 loads <code>A[0]</code></li> <li>thread 1 loads <code>A[1]</code></li> <li>thread 2 loads <code>A[2]</code></li> <li>thread 3 loads <code>A[3]</code></li> </ul> <p>At this point the LSU becomes interesting, because <code>LDR</code> and <code>STR</code> are the instructions that actually interact with the external Python memory model.</p> <p>When reading these parts of the log, pay attention to:</p> <ul> <li><code>LSU State</code></li> <li><code>Core State: WAIT</code></li> </ul> <p>Those two fields tell you when the machine is stalled waiting for memory rather than computing.</p>"},{"location":"report/05-matadd-trace-walkthrough/#step-7-why-stores-show-up-as-memwrite-lines","title":"Step 7: why stores show up as <code>[memwrite]</code> lines","text":"<p>The actual memory writes are not only visible through thread state. They are also explicitly emitted by <code>test/helpers/memory.py</code> as lines like:</p> <pre><code>[memwrite] data cycle=151 lane=0 addr=19 old=0 new=6\n</code></pre> <p>These lines are generated by the Python memory model when it sees a valid data-memory write request.</p> <p>That makes them the clearest proof of \u201cthe kernel has written its results.\u201d</p>"},{"location":"report/05-matadd-trace-walkthrough/#step-8-the-first-result-store-burst-happens-at-cycle-151","title":"Step 8: the first result-store burst happens at cycle 151","text":"<p>The first store burst in the log is:</p> <pre><code>[memwrite] data cycle=151 lane=0 addr=19 old=0 new=6\n[memwrite] data cycle=151 lane=1 addr=18 old=0 new=4\n[memwrite] data cycle=151 lane=2 addr=17 old=0 new=2\n[memwrite] data cycle=151 lane=3 addr=16 old=0 new=0\n</code></pre> <p>This is Core 0 writing results for threads <code>0..3</code>.</p> <p>Notice two things:</p> <ol> <li>the addresses are reversed by lane order in the log output (<code>19, 18, 17, 16</code>)</li> <li> <p>but the values are exactly the expected sums for the first four elements:</p> </li> <li> <p><code>C[0] = 0 + 0 = 0</code></p> </li> <li><code>C[1] = 1 + 1 = 2</code></li> <li><code>C[2] = 2 + 2 = 4</code></li> <li><code>C[3] = 3 + 3 = 6</code></li> </ol> <p>Around this same point, the threads are sitting on:</p> <pre><code>Instruction: STR R7, R6\nCore State: WAIT\nLSU State: WAITING\n</code></pre> <p>That combination tells you the core has already issued the store and is waiting for the memory-side handshake to complete.</p>"},{"location":"report/05-matadd-trace-walkthrough/#step-9-the-second-result-store-burst-happens-at-cycle-158","title":"Step 9: the second result-store burst happens at cycle 158","text":"<p>The second store burst is:</p> <pre><code>[memwrite] data cycle=158 lane=0 addr=23 old=0 new=14\n[memwrite] data cycle=158 lane=1 addr=22 old=0 new=12\n[memwrite] data cycle=158 lane=2 addr=21 old=0 new=10\n[memwrite] data cycle=158 lane=3 addr=20 old=0 new=8\n</code></pre> <p>This is Core 1 writing results for threads <code>4..7</code>:</p> <ul> <li><code>C[4] = 4 + 4 = 8</code></li> <li><code>C[5] = 5 + 5 = 10</code></li> <li><code>C[6] = 6 + 6 = 12</code></li> <li><code>C[7] = 7 + 7 = 14</code></li> </ul> <p>So the two cores finish their store phases in two distinct bursts:</p> <ul> <li>Core 0 writes addresses <code>16..19</code></li> <li>Core 1 writes addresses <code>20..23</code></li> </ul> <p>This is a nice concrete example of how the blocks are split across cores.</p>"},{"location":"report/05-matadd-trace-walkthrough/#step-10-why-there-are-duplicate-looking-memwrite-lines-on-the-next-cycle","title":"Step 10: why there are duplicate-looking memwrite lines on the next cycle","text":"<p>You also see another burst immediately after each first burst:</p> <pre><code>[memwrite] data cycle=152 ... old=6 new=6\n...\n[memwrite] data cycle=159 ... old=14 new=14\n</code></pre> <p>These look redundant because they are redundant from a data-content perspective.</p> <p>The most useful beginner interpretation is:</p> <p>the memory model is still observing asserted write-valid behavior across another cycle, so it logs another write handshake even though the stored value is unchanged.</p> <p>The important lesson is not \u201cthere are two different mathematical writes.\u201d The important lesson is:</p> <ul> <li>the first burst proves the result values were produced</li> <li>the second burst is a protocol-level repeat, not a second different answer</li> </ul>"},{"location":"report/05-matadd-trace-walkthrough/#step-11-end-of-execution","title":"Step 11: end of execution","text":"<p>Near the end of the log, the machine returns to idle and the logger prints:</p> <pre><code>Completed in 178 cycles\n</code></pre> <p>Then the final data-memory table shows:</p> <pre><code>Addr 16..23 = 0, 2, 4, 6, 8, 10, 12, 14\n</code></pre> <p>So the full story of the run is:</p> <ol> <li>launch two 4-thread blocks</li> <li>fetch the kernel</li> <li>compute each thread\u2019s global index</li> <li>load A[i]</li> <li>load B[i]</li> <li>add them into <code>R6</code></li> <li>compute destination address in <code>R7</code></li> <li>store to output buffer</li> <li>return</li> </ol>"},{"location":"report/05-matadd-trace-walkthrough/#what-to-focus-on-when-you-read-this-log-yourself","title":"What to focus on when you read this log yourself","text":"<p>If you reopen the log, do not try to understand every line equally.</p> <p>Read in this order:</p> <ol> <li>initial data-memory dump</li> <li>cycle 7 \u2014 first real fetched instruction</li> <li>the first <code>ADD R0, R0, %threadIdx</code> sequence</li> <li>the first <code>LDR</code> sequence around cycle 73</li> <li>the <code>[memwrite]</code> bursts at cycles 151 and 158</li> <li>final data-memory dump</li> </ol> <p>That reading order gives you the high-level execution story first.</p>"},{"location":"report/05-matadd-trace-walkthrough/#beginner-takeaway","title":"Beginner takeaway","text":"<p>This trace is a good example of SIMD execution in a tiny GPU:</p> <ul> <li>one shared control flow per core</li> <li>one private register file per thread</li> <li>identical instructions across threads</li> <li>different data because <code>%threadIdx</code> and <code>%blockIdx</code> differ</li> <li>memory latency exposed through <code>WAIT</code> and <code>LSU State</code></li> </ul> <p>Once that picture clicks, the log becomes much easier to read.</p>"},{"location":"report/06-matmul-trace-walkthrough/","title":"matmul Execution Trace Walkthrough","text":""},{"location":"report/06-matmul-trace-walkthrough/#goal-of-this-report","title":"Goal of this report","text":"<p>This chapter explains the <code>matmul</code> execution trace in a way that is useful for a Verilog beginner.</p> <p>This kernel is more interesting than <code>matadd</code> because it contains:</p> <ul> <li>address arithmetic</li> <li>a loop</li> <li>a compare instruction</li> <li>a branch instruction</li> <li>repeated loads and multiplies</li> <li>a final store after the loop finishes</li> </ul> <p>All observations below are grounded in a fresh run:</p> <ul> <li>log: <code>test/logs/log_20260402201026.txt</code></li> <li>completed in: <code>491 cycles</code></li> </ul>"},{"location":"report/06-matmul-trace-walkthrough/#what-this-test-is-trying-to-prove","title":"What this test is trying to prove","text":"<p><code>test/test_matmul.py</code> multiplies two 2\u00d72 matrices:</p> <ul> <li>matrix A at addresses <code>0..3</code> = <code>1 2 3 4</code></li> <li>matrix B at addresses <code>4..7</code> = <code>1 2 3 4</code></li> <li>output C at addresses <code>8..11</code>, initially zero</li> </ul> <p>The expected result matrix is:</p> <pre><code>7 10\n15 22\n</code></pre> <p>The final memory dump confirms:</p> <ul> <li><code>data[8..11] = 7, 10, 15, 22</code></li> </ul>"},{"location":"report/06-matmul-trace-walkthrough/#one-very-important-logging-detail","title":"One very important logging detail","text":"<p>Unlike <code>matadd</code>, this test does not log every thread\u2019s full state.</p> <p>It calls:</p> <pre><code>format_cycle(dut, cycles, thread_id=1)\n</code></pre> <p>So the per-cycle trace is filtered to logical thread 1 only.</p> <p>That means:</p> <ul> <li>register dumps are only for thread 1</li> <li>instruction/state lines are only for thread 1</li> <li>but <code>[memwrite]</code> lines still show all four lanes when the store happens</li> </ul> <p>This is why the report focuses on thread 1 as the \u201cmain character,\u201d then uses the final memwrite burst to show that all threads completed.</p>"},{"location":"report/06-matmul-trace-walkthrough/#what-thread-1-is-computing","title":"What thread 1 is computing","text":"<p>For a 2\u00d72 matrix multiply, there are 4 output elements, so there are 4 threads.</p> <p>Thread 1 corresponds to global index <code>i = 1</code>.</p> <p>The kernel computes:</p> <ul> <li><code>row = i // N</code></li> <li><code>col = i % N</code></li> </ul> <p>with <code>N = 2</code>.</p> <p>So for thread 1:</p> <ul> <li><code>row = 0</code></li> <li><code>col = 1</code></li> </ul> <p>That means thread 1 is responsible for output element:</p> <pre><code>C[0,1]\n</code></pre> <p>Mathematically:</p> <pre><code>C[0,1] = A[0,0] * B[0,1] + A[0,1] * B[1,1]\n       = 1 * 2 + 2 * 4\n       = 10\n</code></pre> <p>The final trace shows thread 1 ends with <code>R8 = 10</code>, which matches that expected dot product.</p>"},{"location":"report/06-matmul-trace-walkthrough/#how-to-read-the-scheduler-rhythm","title":"How to read the scheduler rhythm","text":"<p>Just like <code>matadd</code>, each ISA instruction goes through the same scheduler stages:</p> <pre><code>FETCH -&gt; DECODE -&gt; REQUEST -&gt; WAIT -&gt; EXECUTE -&gt; UPDATE\n</code></pre> <p>This is why even simple instructions like <code>CONST</code> appear across multiple cycles.</p> <p>For example, around cycles <code>40..45</code>, the trace shows <code>CONST R2, #2</code> moving through:</p> <ul> <li>FETCH</li> <li>DECODE</li> <li>REQUEST</li> <li>WAIT</li> <li>EXECUTE</li> <li>UPDATE</li> </ul> <p>Only after that does <code>R2</code> actually become <code>2</code> in the register dump.</p> <p>That is the right beginner mental model:</p> <p>the trace is showing the scheduler\u2019s control steps, not just the instruction list in assembly.</p>"},{"location":"report/06-matmul-trace-walkthrough/#step-1-startup-and-first-real-instruction","title":"Step 1: startup and first real instruction","text":"<p>As in <code>matadd</code>, the early cycles are not yet \u201creal compute.\u201d</p> <p>At cycles <code>0..6</code>, thread 1 is still showing <code>NOP</code> while fetch is warming up.</p> <p>At cycle 7, the first real instruction appears:</p> <pre><code>PC: 0\nInstruction: MUL R0, %blockIdx, %blockDim\n</code></pre> <p>Then the second instruction is:</p> <pre><code>ADD R0, R0, %threadIdx\n</code></pre> <p>For thread 1, that gives:</p> <pre><code>R0 = 1\n</code></pre> <p>which is its global thread index.</p>"},{"location":"report/06-matmul-trace-walkthrough/#step-2-setting-up-constants-and-thread-coordinates","title":"Step 2: setting up constants and thread coordinates","text":"<p>The next few instructions establish the constants needed for matrix math:</p> <ul> <li><code>R1 = 1</code>  \u2192 increment value for loop counter</li> <li><code>R2 = 2</code>  \u2192 matrix dimension N</li> <li><code>R3 = 0</code>  \u2192 base of A</li> <li><code>R4 = 4</code>  \u2192 base of B</li> <li><code>R5 = 8</code>  \u2192 base of C</li> </ul> <p>Then the kernel computes thread-specific coordinates:</p> <ul> <li><code>DIV R6, R0, R2</code> \u2192 row</li> <li><code>MUL R7, R6, R2</code></li> <li><code>SUB R7, R0, R7</code> \u2192 col</li> </ul> <p>For thread 1 the resulting interpretation is:</p> <ul> <li><code>R6 = 0</code> \u2192 row 0</li> <li><code>R7 = 1</code> \u2192 col 1</li> </ul> <p>This exactly matches the expected output location <code>C[0,1]</code>.</p>"},{"location":"report/06-matmul-trace-walkthrough/#step-3-the-loop-body-starts-at-pc-12","title":"Step 3: the loop body starts at PC 12","text":"<p>The loop body begins at program counter 12.</p> <p>Around cycle 296, after the first branch-back, the trace shows:</p> <pre><code>PC: 12\nInstruction: MUL R10, R6, R2\n</code></pre> <p>That is the first line of the loop body.</p> <p>From there the loop computes two addresses:</p> <ol> <li>address into A using <code>row * N + k + baseA</code></li> <li>address into B using <code>k * N + col + baseB</code></li> </ol> <p>Then it performs:</p> <ul> <li><code>LDR R10, R10</code></li> <li><code>LDR R11, R11</code></li> <li><code>MUL R12, R10, R11</code></li> <li><code>ADD R8, R8, R12</code></li> </ul> <p>That is the classic dot-product inner loop.</p>"},{"location":"report/06-matmul-trace-walkthrough/#step-4-understanding-one-loop-iteration-for-thread-1","title":"Step 4: understanding one loop iteration for thread 1","text":"<p>Thread 1 executes two loop iterations because <code>N = 2</code>.</p>"},{"location":"report/06-matmul-trace-walkthrough/#first-iteration-k-0","title":"First iteration (<code>k = 0</code>)","text":"<p>The relevant values become:</p> <ul> <li>load <code>A[0,0] = 1</code></li> <li>load <code>B[0,1] = 2</code></li> <li>multiply \u2192 <code>1 * 2 = 2</code></li> <li>accumulate \u2192 <code>R8 = 2</code></li> </ul> <p>You can see this state clearly near the first branch-back region. Around cycles <code>284..291</code>, thread 1 has:</p> <ul> <li><code>R8 = 2</code></li> <li><code>R9 = 1</code></li> <li><code>R10 = 1</code></li> <li><code>R11 = 2</code></li> <li><code>R12 = 2</code></li> </ul> <p>That register snapshot is exactly what you want after the first dot-product term has been accumulated.</p>"},{"location":"report/06-matmul-trace-walkthrough/#first-branch-decision-loop-continues","title":"First branch decision: loop continues","text":"<p>At cycle 284 the trace is approaching the loop branch logic:</p> <pre><code>PC: 24\nInstruction: CMP R9, R2\n</code></pre> <p>At this point:</p> <ul> <li><code>R9 = 1</code></li> <li><code>R2 = 2</code></li> </ul> <p>So the comparison means \u201cis <code>k</code> still less than <code>N</code>?\u201d</p> <p>The following branch sequence is visible in cycles <code>285..291</code>:</p> <ul> <li>branch instruction fetched and executed</li> <li>at cycle 291, <code>PC</code> jumps back to <code>12</code></li> </ul> <p>That jump back to <code>PC: 12</code> is the proof that the loop continues for another iteration.</p>"},{"location":"report/06-matmul-trace-walkthrough/#step-5-second-iteration-k-1","title":"Step 5: second iteration (<code>k = 1</code>)","text":"<p>In the second loop round, thread 1 should use:</p> <ul> <li><code>A[0,1] = 2</code></li> <li><code>B[1,1] = 4</code></li> </ul> <p>So the second product is:</p> <pre><code>2 * 4 = 8\n</code></pre> <p>Adding that to the previous accumulator value gives:</p> <pre><code>R8 = 2 + 8 = 10\n</code></pre> <p>By the time the loop-exit compare happens later, the trace confirms exactly that state.</p> <p>Near cycles <code>432..447</code>, thread 1 shows:</p> <ul> <li><code>R8 = 10</code></li> <li><code>R9 = 2</code></li> <li><code>R10 = 2</code></li> <li><code>R11 = 4</code></li> <li><code>R12 = 8</code></li> </ul> <p>This is the clearest \u201cthe dot product is finished\u201d snapshot in the trace.</p>"},{"location":"report/06-matmul-trace-walkthrough/#step-6-second-branch-decision-means-loop-exit","title":"Step 6: second branch decision means loop exit","text":"<p>The second compare/branch sequence appears around cycles <code>432..447</code>.</p> <p>This time the key values are:</p> <ul> <li><code>R9 = 2</code></li> <li><code>R2 = 2</code></li> </ul> <p>So <code>k</code> has reached <code>N</code>.</p> <p>The branch no longer goes back to the loop body.</p> <p>You can see the difference in the PC progression:</p> <ul> <li>after the branch update, the trace moves to <code>PC: 25</code>, then <code>PC: 26</code></li> <li>it does not jump back to <code>PC: 12</code></li> </ul> <p>That is how the log shows \u201cloop exit\u201d without printing a high-level English sentence.</p> <p>For beginners, this is the most important branch-reading trick in the whole file:</p> <p>watch where the PC goes after the branch completes.</p> <ul> <li>if it returns to <code>PC 12</code>, the loop continues</li> <li>if it advances to <code>PC 25/26</code>, the loop is over</li> </ul>"},{"location":"report/06-matmul-trace-walkthrough/#step-7-computing-the-output-address","title":"Step 7: computing the output address","text":"<p>After the loop exits, the kernel computes the destination address for C.</p> <p>Around cycle 459 the trace shows:</p> <pre><code>PC: 26\nInstruction: ADD R9, R5, R0\n</code></pre> <p>At this moment:</p> <ul> <li><code>R5 = 8</code>  \u2192 base of output matrix</li> <li><code>R0 = 1</code>  \u2192 thread/global output index</li> </ul> <p>So thread 1 computes:</p> <pre><code>R9 = 9\n</code></pre> <p>That is exactly the correct address for output element <code>C[0,1]</code>.</p>"},{"location":"report/06-matmul-trace-walkthrough/#step-8-final-store-and-the-all-thread-memwrite-burst","title":"Step 8: final store and the all-thread memwrite burst","text":"<p>At cycle 464 the next real instruction becomes:</p> <pre><code>Instruction: STR R9, R8\n</code></pre> <p>For thread 1, that means:</p> <ul> <li>store to address <code>R9 = 9</code></li> <li>store value <code>R8 = 10</code></li> </ul> <p>Then the <code>[memwrite]</code> lines show that all four lanes write their results at cycle 471:</p> <pre><code>[memwrite] data cycle=471 lane=0 addr=11 old=0 new=22\n[memwrite] data cycle=471 lane=1 addr=10 old=0 new=15\n[memwrite] data cycle=471 lane=2 addr=9 old=0 new=10\n[memwrite] data cycle=471 lane=3 addr=8 old=0 new=7\n</code></pre> <p>Even though the per-cycle trace only logs thread 1, these four memwrite lines reveal the final outputs of all threads:</p> <ul> <li>addr 8  \u2192 7</li> <li>addr 9  \u2192 10</li> <li>addr 10 \u2192 15</li> <li>addr 11 \u2192 22</li> </ul> <p>So the final matrix result is visible in one compact burst.</p> <p>Just like <code>matadd</code>, the next cycle also shows repeated writes with unchanged values:</p> <pre><code>[memwrite] data cycle=472 ... old=22 new=22\n...\n</code></pre> <p>The safest interpretation is again that the write handshake remains visible for another cycle, not that the mathematical result changed.</p>"},{"location":"report/06-matmul-trace-walkthrough/#step-9-return-and-completion","title":"Step 9: return and completion","text":"<p>Near the end of the log, thread 1 reaches:</p> <pre><code>PC: 27\nInstruction: RET\nCore State: DONE\nCore Done: 1\n</code></pre> <p>Then the test prints:</p> <pre><code>Completed in 491 cycles\n</code></pre> <p>And the final memory table confirms:</p> <pre><code>data[8..11] = 7, 10, 15, 22\n</code></pre>"},{"location":"report/06-matmul-trace-walkthrough/#what-to-focus-on-when-you-read-this-log-yourself","title":"What to focus on when you read this log yourself","text":"<p>A good reading order is:</p> <ol> <li>initial data-memory dump</li> <li>cycle 7 \u2014 first real fetched instruction</li> <li>the <code>DIV</code> / <code>SUB</code> steps that establish row and column</li> <li>the first <code>LDR R10</code> / <code>LDR R11</code> sequence</li> <li>the first <code>CMP</code> + branch-back sequence around cycles <code>284..291</code></li> <li>the second <code>CMP</code> + branch-exit sequence around cycles <code>432..447</code></li> <li>the final <code>STR R9, R8</code> sequence around cycles <code>464..471</code></li> <li>final data-memory dump</li> </ol> <p>That reading order lets you see the kernel as:</p> <ul> <li>setup</li> <li>loop iteration 1</li> <li>loop iteration 2</li> <li>loop exit</li> <li>final writeback</li> </ul>"},{"location":"report/06-matmul-trace-walkthrough/#beginner-takeaway","title":"Beginner takeaway","text":"<p>This trace is valuable because it shows how a tiny GPU kernel can still express a real control-flow pattern:</p> <ul> <li>per-thread indexing</li> <li>address generation</li> <li>repeated loads from memory</li> <li>accumulation in a register</li> <li>a compare-and-branch loop</li> <li>final store to global memory</li> </ul> <p>If <code>matadd</code> teaches \u201csame instruction, different thread data,\u201d then <code>matmul</code> teaches the next layer:</p> <p>a GPU thread can also run a local sequential algorithm, and the trace shows exactly how that algorithm unfolds through fetch, wait, execute, and update phases.</p>"},{"location":"tooling/codegen/","title":"ISA codegen","text":"<p>The canonical ISA spec lives in <code>docs/isa/instructions.yaml</code>. Three artefacts are generated from it:</p> Artefact Path Consumer C++ decode table <code>sim/cppref/include/opengpu/isa_table.hpp</code> C++ reference simulator customasm grammar <code>tools/asm/opengpu.asm</code> hex assembler Markdown reference <code>docs/isa/reference.md</code> this site <p>CI runs <code>python -m tools.codegen.regen --check</code> and fails any PR that hand-edits a generated file or forgets to re-run the codegen after changing the YAML.</p>"},{"location":"tooling/codegen/#regenerate-locally","title":"Regenerate locally","text":"<pre><code>python -m tools.codegen.regen           # write outputs\npython -m tools.codegen.regen --check   # CI mode: exit 1 on drift\n</code></pre>"},{"location":"tooling/codegen/#adding-a-new-instruction","title":"Adding a new instruction","text":"<ol> <li>Edit <code>docs/isa/instructions.yaml</code>. Pick an unused opcode (<code>0x0</code>\u2013<code>0xF</code>),    give it an encoding <code>type</code> (<code>R</code>, <code>I</code>, <code>B</code>, <code>Z</code>), document <code>syntax</code>    and <code>semantics</code>, and list the <code>control</code> signals it asserts.</li> <li>Add the matching case to <code>src/decoder.sv</code>. The control-signal names    must match the YAML exactly \u2014 the loader fails fast otherwise.</li> <li>Run <code>python -m tools.codegen.regen</code>. The C++ table, the asm    grammar, and the ISA reference page are    refreshed automatically.</li> <li>Add a Catch2 test in <code>sim/cppref/tests/</code> and a cocotb test in    <code>test/</code> if the instruction has non-trivial semantics.</li> <li><code>python -m tools.codegen.regen --check</code> should exit clean before you    commit.</li> </ol>"},{"location":"tooling/codegen/#api","title":"API","text":""},{"location":"tooling/codegen/#tools.codegen.isa_loader","title":"tools.codegen.isa_loader","text":"<p>Typed loader / validator for <code>docs/isa/instructions.yaml</code>.</p> Why a dedicated module <ul> <li>The YAML is the single source of truth for the assembler, the C++ ref   simulator, the docs site, and (eventually) DiffTest. Validating it once   here keeps the templates dumb (no defensive logic in Jinja).</li> <li>Validation surfaces drift from RTL early: e.g. if someone adds a new   control signal in <code>decoder.sv</code> they must declare it here, otherwise   codegen fails loudly.</li> </ul> <p>The loader has no third-party deps beyond <code>PyYAML</code> (already pinned via the toolchain image).</p>"},{"location":"tooling/codegen/#tools.codegen.isa_loader.IsaError","title":"IsaError","text":"<p>               Bases: <code>ValueError</code></p> <p>Raised when the ISA YAML is structurally invalid.</p>"},{"location":"tooling/codegen/#tools.codegen.isa_loader.load","title":"load","text":"<pre><code>load(path: str | Path) -&gt; Isa\n</code></pre> <p>Load and validate the ISA YAML.</p>"},{"location":"tooling/codegen/#tools.codegen.regen","title":"tools.codegen.regen","text":"<p>Regenerate ISA-derived files from <code>docs/isa/instructions.yaml</code>.</p> <p>CLI:     python -m tools.codegen.regen           # write outputs     python -m tools.codegen.regen --check   # fail (exit 1) if outputs would change</p> <p>The <code>--check</code> mode is what CI runs: it ensures contributors regenerate artefacts whenever the YAML changes, instead of hand-editing them.</p>"},{"location":"tooling/codegen/#tools.codegen.render_isa_md","title":"tools.codegen.render_isa_md","text":"<p>Render the canonical ISA YAML to a Markdown reference page.</p> <p>Output: <code>docs/isa/reference.md</code> (consumed by mkdocs build).</p> <p>This page is generated. CI re-runs the renderer with <code>--check</code> and fails if the checked-in copy drifts (mirrors the codegen --check pattern used for the C++ table and the customasm ruleset).</p>"},{"location":"tooling/difftest/","title":"DiffTest harness","text":"<p><code>tools/difftest/</code> compares two retire-instruction streams in lockstep: the RTL (cocotb) and the C++ reference simulator. The first divergence aborts the test with a precise message:</p> <pre><code>DiffTest mismatch at retire #42 field='rd_val'\n  RTL: RetireRecord(tid=2, pc=12, instr=0x4422, op='ADD', rd=2, rd_val=9, ...)\n  REF: RetireRecord(tid=2, pc=12, instr=0x4422, op='ADD', rd=2, rd_val=8, ...)\n</code></pre>"},{"location":"tooling/difftest/#workflow","title":"Workflow","text":"<ol> <li>Run a kernel through the reference simulator    with <code>--trace ref.jsonl</code>.</li> <li>From the cocotb test, write one matching JSON record per retired    instruction (same schema; see Trace schema).</li> <li>Call <code>diff_streams(iter_jsonl(rtl), iter_jsonl(ref))</code>.</li> </ol> <pre><code>from tools.difftest import diff_streams, iter_jsonl, run_refsim\n\nrun_refsim(\n    \"build/cppref/opengpu-refsim\",\n    program=\"program.hex\",\n    data=\"data.hex\",\n    threads=8,\n    trace=\"ref.jsonl\",\n)\nn = diff_streams(iter_jsonl(\"rtl.jsonl\"), iter_jsonl(\"ref.jsonl\"))\nprint(f\"DiffTest OK ({n} records compared)\")\n</code></pre>"},{"location":"tooling/difftest/#api","title":"API","text":""},{"location":"tooling/difftest/#tools.difftest","title":"tools.difftest","text":"<p>DiffTest: lockstep equivalence check between RTL (cocotb) and the C++ reference simulator (<code>opengpu-refsim</code>).</p> Design <ul> <li> <p>The refsim emits one JSON line per retired instruction to its trace   file (see <code>sim/cppref/src/trace.cpp</code>). Each record looks like::</p> <p>{\"tick\":N,\"tid\":T,\"pc\":P,\"instr\":I,\"op\":\"ADD\",    \"rd\":1,\"rd_val\":42,\"mem_w\":null,\"nzp\":0,\"done\":false}</p> </li> <li> <p>On the cocotb side, an integration writes one record per retired   instruction with the same schema.  <code>DiffStream</code> walks the two   streams in lockstep keyed on <code>(tick, tid)</code> and raises   :class:<code>DiffMismatch</code> on the first divergence, with full context.</p> </li> <li> <p>This module deliberately knows nothing about the cocotb fixtures - it   just consumes two JSONL streams.  That keeps the harness usable from   unit tests, CI, and ad-hoc debugging.</p> </li> </ul>"},{"location":"tooling/difftest/#tools.difftest.RetireRecord","title":"RetireRecord  <code>dataclass</code>","text":"<p>One retired instruction from either RTL or refsim.</p>"},{"location":"tooling/difftest/#tools.difftest.DiffMismatch","title":"DiffMismatch","text":"<p>               Bases: <code>AssertionError</code></p> <p>Raised on the first RTL/refsim divergence.</p> <p>The message contains the offending field and both records, which is typically enough for a developer to localise the bug to a single instruction.</p>"},{"location":"tooling/difftest/#tools.difftest.iter_jsonl","title":"iter_jsonl","text":"<pre><code>iter_jsonl(path: Path | str) -&gt; Iterator[RetireRecord]\n</code></pre> <p>Stream records from a JSONL trace file.</p> <p>Blank lines are ignored so that the same parser works on refsim output and on hand-crafted golden traces.</p>"},{"location":"tooling/difftest/#tools.difftest.run_refsim","title":"run_refsim","text":"<pre><code>run_refsim(refsim: Path | str, *, program: Path | str, data: Path | str | None = None, threads: int, blocks: int = 1, trace: Path | str, max_steps: int = 10000, mem_size: int | None = None) -&gt; None\n</code></pre> <p>Invoke <code>opengpu-refsim</code> and write its trace to <code>trace</code>.</p> <p>Parameters mirror the CLI in <code>sim/cppref/src/main.cpp</code>.  Raises :class:<code>subprocess.CalledProcessError</code> if refsim exits non-zero so cocotb tests fail fast on backend errors.</p>"},{"location":"tooling/difftest/#tools.difftest.diff_streams","title":"diff_streams","text":"<pre><code>diff_streams(rtl: Iterable[RetireRecord], ref: Iterable[RetireRecord], *, fields: tuple[str, ...] = COMPARED_FIELDS) -&gt; int\n</code></pre> <p>Compare two retire streams in lockstep.</p> <p>Returns the number of records compared.  Raises :class:<code>DiffMismatch</code> on the first field divergence and :class:<code>AssertionError</code> if one stream is shorter than the other.</p>"}]}