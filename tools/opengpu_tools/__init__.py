"""OpenGPU tools — assembler, ISA codegen, runtime helpers.

This package will be progressively populated; see
`docs/isa/instructions.yaml` for the canonical ISA spec and
`tools/codegen/` for the generators.
"""

__version__ = "0.1.0"
